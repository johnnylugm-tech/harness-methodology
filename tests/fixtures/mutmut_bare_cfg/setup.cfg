# Round 35 站0 — the shape that broke, copied from a live project.
#
# A setup.cfg that EXISTS but carries no [tool:pytest] and no [mutmut]: the
# project reaches its own source through a conftest.py sys.path insertion, so
# it never needed to declare testpaths or pythonpath anywhere.
#
# `mutmut_smoke/setup.cfg` — the other fixture — declares both, which is why
# the workdir bootstrap defect survived every run of the real-mutmut
# integration test: that fixture is the well-configured shape.
[coverage:run]
branch = False
