"""
harness/tool_runners.py — Solution B: independent tool execution for cross-validation.

Each tool is run by the harness (not the agent) so scores cannot be fabricated by
writing stub files.  Results are written to .sessi-work/harness_verification/ as an
audit trail.

API
───
  run_tool(tool, project_root, *, timeout_override=None) → (output: str, returncode: int)
  compute_tool_score(tool, output, returncode) → float | None

Return-code conventions (negative = harness-internal, not tool exit codes):
  -1  tool is on the skip list (mutmut, scancode) — too slow / complex
  -2  subprocess timed out
  -3  tool executable not found
  -4  unexpected subprocess error
"""

from __future__ import annotations

import ast
import re
import subprocess
from typing import Optional

# Tools that are too slow or complex for inline cross-validation.
# They are still covered by Solution-A content validation.
_SKIP_TOOLS: frozenset[str] = frozenset({"mutmut", "scancode"})

# Default per-tool timeouts (seconds).
_DEFAULT_TIMEOUTS: dict[str, int] = {
    "ruff":             30,
    "mypy":             60,
    "pyright":          60,
    "pytest-cov":       120,
    "pytest":           120,
    "gitleaks":         30,
    "bandit":           60,
    "radon-cc":         30,
    "radon-mi":         30,
    "pytest-benchmark": 180,
    "ast-assertions":   30,
    "ast-error-handling": 30,
    "ast-docstrings":   30,
    "pytest-cov-integration": 180,
}

# Source directories scanned for file-level error-handling coverage.
_SRC_DIRS: tuple[str, ...] = ("03-development/src", "src")
from inspect import signature as _mutmut_signature
from typing import Annotated
from typing import Callable
from typing import ClassVar


MutantDict = Annotated[dict[str, Callable], "Mutant"]


def _mutmut_trampoline(orig, mutants, call_args, call_kwargs, self_arg = None):
    """Forward call to original or mutated function, depending on the environment"""
    import os
    mutant_under_test = os.environ['MUTANT_UNDER_TEST']
    if mutant_under_test == 'fail':
        from mutmut.__main__ import MutmutProgrammaticFailException
        raise MutmutProgrammaticFailException('Failed programmatically')      
    elif mutant_under_test == 'stats':
        from mutmut.__main__ import record_trampoline_hit
        record_trampoline_hit(orig.__module__ + '.' + orig.__name__)
        result = orig(*call_args, **call_kwargs)
        return result
    prefix = orig.__module__ + '.' + orig.__name__ + '__mutmut_'
    if not mutant_under_test.startswith(prefix):
        result = orig(*call_args, **call_kwargs)
        return result
    mutant_name = mutant_under_test.rpartition('.')[-1]
    if self_arg:
        # call to a class method where self is not bound
        result = mutants[mutant_name](self_arg, *call_args, **call_kwargs)
    else:
        result = mutants[mutant_name](*call_args, **call_kwargs)
    return result


def x_run_tool__mutmut_orig(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_1(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool not in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_2(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "XXXX", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_3(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", +1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_4(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -2

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_5(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool != "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_6(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "XXast-assertionsXX":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_7(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "AST-ASSERTIONS":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_8(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(None)
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_9(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(None))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_10(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool != "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_11(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "XXast-error-handlingXX":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_12(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "AST-ERROR-HANDLING":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_13(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(None)
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_14(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(None))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_15(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool != "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_16(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "XXast-docstringsXX":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_17(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "AST-DOCSTRINGS":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_18(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(None)

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_19(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(None))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_20(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = None
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_21(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_22(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(None, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_23(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, None)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_24(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_25(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, )
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_26(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 31)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_27(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = None

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_28(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(None)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_29(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = None

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_30(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "XXruffXX": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_31(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "RUFF": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_32(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "XXruffXX", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_33(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "RUFF", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_34(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "XXcheckXX", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_35(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "CHECK", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_36(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "XX--output-formatXX", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_37(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--OUTPUT-FORMAT", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_38(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "XXjsonXX",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_39(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "JSON",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_40(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "XX--exit-zeroXX",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_41(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--EXIT-ZERO",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_42(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "XXmypyXX": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_43(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "MYPY": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_44(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "XXmypyXX", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_45(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "MYPY", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_46(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "XX--ignore-missing-importsXX",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_47(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--IGNORE-MISSING-IMPORTS",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_48(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "XX--no-color-outputXX",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_49(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--NO-COLOR-OUTPUT",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_50(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "XX--no-error-summaryXX",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_51(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--NO-ERROR-SUMMARY",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_52(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "XXpyrightXX": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_53(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "PYRIGHT": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_54(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "XXpyrightXX", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_55(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "PYRIGHT", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_56(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "XX--outputjsonXX",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_57(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--OUTPUTJSON",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_58(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "XXpytest-covXX": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_59(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "PYTEST-COV": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_60(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "XXpytestXX", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_61(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "PYTEST", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_62(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "XX--covXX", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_63(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--COV", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_64(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "XX--cov-report=term-missingXX",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_65(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--COV-REPORT=TERM-MISSING",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_66(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "XX-qXX", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_67(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-Q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_68(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "XX--tb=noXX", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_69(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--TB=NO", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_70(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "XX--no-headerXX",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_71(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--NO-HEADER",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_72(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "XXpytestXX": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_73(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "PYTEST": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_74(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "XXpytestXX", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_75(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "PYTEST", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_76(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "XX-qXX", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_77(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-Q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_78(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "XX--tb=noXX", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_79(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--TB=NO", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_80(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "XX--no-headerXX",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_81(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--NO-HEADER",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_82(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "XXgitleaksXX": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_83(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "GITLEAKS": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_84(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "XXgitleaksXX", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_85(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "GITLEAKS", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_86(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "XXdetectXX",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_87(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "DETECT",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_88(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "XX--sourceXX", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_89(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--SOURCE", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_90(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "XXbanditXX": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_91(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "BANDIT": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_92(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "XXbanditXX", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_93(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "BANDIT", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_94(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "XX-rXX", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_95(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-R", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_96(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "XX-fXX", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_97(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-F", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_98(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "XXjsonXX",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_99(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "JSON",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_100(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "XX--exit-zeroXX",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_101(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--EXIT-ZERO",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_102(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "XXradon-ccXX": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_103(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "RADON-CC": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_104(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "XXradonXX", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_105(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "RADON", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_106(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "XXccXX", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_107(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "CC", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_108(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "XX-jXX",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_109(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-J",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_110(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "XX--minXX", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_111(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--MIN", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_112(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "XXAXX",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_113(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "a",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_114(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "XXradon-miXX": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_115(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "RADON-MI": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_116(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "XXradonXX", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_117(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "RADON", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_118(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "XXmiXX", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_119(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "MI", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_120(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "XX-jXX",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_121(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-J",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_122(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "XXpytest-benchmarkXX": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_123(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "PYTEST-BENCHMARK": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_124(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "XXpytestXX", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_125(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "PYTEST", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_126(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "XX--benchmark-onlyXX",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_127(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--BENCHMARK-ONLY",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_128(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "XX--benchmark-disable-gcXX",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_129(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--BENCHMARK-DISABLE-GC",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_130(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "XX--benchmark-columnsXX", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_131(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--BENCHMARK-COLUMNS", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_132(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "XXmean,maxXX",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_133(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "MEAN,MAX",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_134(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "XX--tbXX", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_135(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--TB", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_136(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "XXnoXX",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_137(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "NO",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_138(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "XX-qXX",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_139(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-Q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_140(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "XXpytest-cov-integrationXX": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_141(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "PYTEST-COV-INTEGRATION": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_142(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "XXpytestXX", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_143(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "PYTEST", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_144(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "XX03-development/tests/integrationXX",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_145(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-DEVELOPMENT/TESTS/INTEGRATION",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_146(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "XX--cov=03-development/srcXX", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_147(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--COV=03-DEVELOPMENT/SRC", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_148(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "XX--cov-report=term-missingXX",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_149(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--COV-REPORT=TERM-MISSING",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_150(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "XX-qXX", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_151(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-Q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_152(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "XX--tb=noXX", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_153(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--TB=NO", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_154(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "XX--no-headerXX",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_155(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--NO-HEADER",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_156(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = None
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_157(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(None)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_158(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_159(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "XXXX", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_160(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", +1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_161(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -2  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_162(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = None
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_163(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            None,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_164(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=None,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_165(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=None,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_166(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=None,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_167(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=None,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_168(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_169(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_170(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_171(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_172(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_173(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=False,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_174(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=False,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_175(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = None
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_176(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout - proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_177(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", +2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_178(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -3
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_179(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", +3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_180(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -4
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -4


def x_run_tool__mutmut_181(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", +4


def x_run_tool__mutmut_182(
    tool: str,
    project_root: str,
    *,
    timeout_override: Optional[int] = None,
) -> tuple[str, int]:
    """Execute *tool* against *project_root* and return (stdout+stderr, returncode).

    Returns (message, negative_code) when the tool is skipped/unavailable:
      ("", -1)  — skipped tool
      ("TIMEOUT…", -2) — subprocess timed out
      ("Tool not found…", -3) — executable missing
      ("Error: …", -4) — unexpected exception
    """
    if tool in _SKIP_TOOLS:
        return "", -1

    # ast-assertions / ast-error-handling / ast-docstrings are computed in-process (AST scan).
    if tool == "ast-assertions":
        return _run_ast_assertions(str(project_root))
    if tool == "ast-error-handling":
        return _run_ast_error_handling(str(project_root))
    if tool == "ast-docstrings":
        return _run_ast_docstrings(str(project_root))

    timeout = timeout_override if timeout_override is not None else _DEFAULT_TIMEOUTS.get(tool, 30)
    root = str(project_root)

    # Build command per tool.
    cmds: dict[str, list[str]] = {
        "ruff": [
            "ruff", "check", root,
            "--output-format", "json",
            "--exit-zero",
        ],
        "mypy": [
            "mypy", root,
            "--ignore-missing-imports",
            "--no-color-output",
            "--no-error-summary",
        ],
        "pyright": [
            "pyright", root,
            "--outputjson",
        ],
        "pytest-cov": [
            "pytest", root,
            "--cov", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
        "pytest": [
            "pytest", root,
            "-q", "--tb=no", "--no-header",
        ],
        "gitleaks": [
            "gitleaks", "detect",
            "--source", root,
            # No --exit-code override: exit 0 = clean, exit 1 = leaks found.
            # Overriding to 0 would make the scorer always return 100.
        ],
        "bandit": [
            "bandit", "-r", root,
            "-f", "json",
            "--exit-zero",  # always exit 0 so returncode doesn't mask JSON output
        ],
        "radon-cc": [
            "radon", "cc", root,
            "-j",           # JSON output
            "--min", "A",   # include all grades (A-F)
        ],
        "radon-mi": [
            "radon", "mi", root,
            "-j",           # JSON output
        ],
        # --benchmark-only: run only tests using the `benchmark` fixture.
        # If none exist, pytest exits with code 5 (no tests collected) → scorer returns None.
        # Text output (not --benchmark-json) so results flow through stdout capture.
        "pytest-benchmark": [
            "pytest", root,
            "--benchmark-only",
            "--benchmark-disable-gc",
            "--benchmark-columns", "mean,max",
            "--tb", "no",
            "-q",
        ],
        # Integration coverage: run only the integration suite and measure real
        # line coverage of the source tree (NOT pass-rate).  Missing suite →
        # pytest exits 4/5 and the cov table is absent → _score_pytest returns 0
        # → cross-validation blocks (a passing agent score is then unverifiable).
        "pytest-cov-integration": [
            "pytest", "03-development/tests/integration",
            "--cov=03-development/src", "--cov-report=term-missing",
            "-q", "--tb=no", "--no-header",
        ],
    }

    cmd = cmds.get(tool)
    if not cmd:
        return "", -1  # Unknown tool — skip

    try:
        proc = subprocess.run(
            cmd,
            capture_output=True,
            text=True,
            timeout=timeout,
            cwd=root,
        )
        combined = (proc.stdout + proc.stderr).strip()
        return combined, proc.returncode
    except subprocess.TimeoutExpired:
        return f"TIMEOUT: {tool} exceeded {timeout}s", -2
    except FileNotFoundError:
        return f"Tool not found: {tool}", -3
    except Exception as exc:  # pylint: disable=broad-except
        return f"Error running {tool}: {exc}", -5

x_run_tool__mutmut_mutants : ClassVar[MutantDict] = {
'x_run
    'x_run_tool__mutmut_148': x_run_tool__mutmut_148, 
    'x_run_tool__mutmut_149': x_run_tool__mutmut_149, 
    'x_run_tool__mutmut_150': x_run_tool__mutmut_150, 
    'x_run_tool__mutmut_151': x_run_tool__mutmut_151, 
    'x_run_tool__mutmut_152': x_run_tool__mutmut_152, 
    'x_run_tool__mutmut_153': x_run_tool__mutmut_153, 
    'x_run_tool__mutmut_154': x_run_tool__mutmut_154, 
    'x_run_tool__mutmut_155': x_run_tool__mutmut_155, 
    'x_run_tool__mutmut_156': x_run_tool__mutmut_156, 
    'x_run_tool__mutmut_157': x_run_tool__mutmut_157, 
    'x_run_tool__mutmut_158': x_run_tool__mutmut_158, 
    'x_run_tool__mutmut_159': x_run_tool__mutmut_159, 
    'x_run_tool__mutmut_160': x_run_tool__mutmut_160, 
    'x_run_tool__mutmut_161': x_run_tool__mutmut_161, 
    'x_run_tool__mutmut_162': x_run_tool__mutmut_162, 
    'x_run_tool__mutmut_163': x_run_tool__mutmut_163, 
    'x_run_tool__mutmut_164': x_run_tool__mutmut_164, 
    'x_run_tool__mutmut_165': x_run_tool__mutmut_165, 
    'x_run_tool__mutmut_166': x_run_tool__mutmut_166, 
    'x_run_tool__mutmut_167': x_run_tool__mutmut_167, 
    'x_run_tool__mutmut_168': x_run_tool__mutmut_168, 
    'x_run_tool__mutmut_169': x_run_tool__mutmut_169, 
    'x_run_tool__mutmut_170': x_run_tool__mutmut_170, 
    'x_run_tool__mutmut_171': x_run_tool__mutmut_171, 
    'x_run_tool__mutmut_172': x_run_tool__mutmut_172, 
    'x_run_tool__mutmut_173': x_run_tool__mutmut_173, 
    'x_run_tool__mutmut_174': x_run_tool__mutmut_174, 
    'x_run_tool__mutmut_175': x_run_tool__mutmut_175, 
    'x_run_tool__mutmut_176': x_run_tool__mutmut_176, 
    'x_run_tool__mutmut_177': x_run_tool__mutmut_177, 
    'x_run_tool__mutmut_178': x_run_tool__mutmut_178, 
    'x_run_tool__mutmut_179': x_run_tool__mutmut_179, 
    'x_run_tool__mutmut_180': x_run_tool__mutmut_180, 
    'x_run_tool__mutmut_181': x_run_tool__mutmut_181, 
    'x_run_tool__mutmut_182': x_run_tool__mutmut_182
}

def run_tool(*args, **kwargs):
    result = _mutmut_trampoline(x_run_tool__mutmut_orig, x_run_tool__mutmut_mutants, args, kwargs)
    return result 

run_tool.__signature__ = _mutmut_signature(x_run_tool__mutmut_orig)
x_run_tool__mutmut_orig.__name__ = 'x_run_tool'


# Test directories scanned for assertion-quality analysis (first existing wins is
# NOT used — all are scanned and merged).
_TEST_DIRS: tuple[str, ...] = ("tests", "03-development/tests")


def x__function_has_assertion__mutmut_orig(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_1(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(None):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_2(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return False
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_3(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = None
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_4(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = None
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_5(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") and name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_6(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith(None) or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_7(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("XXassertXX") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_8(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("ASSERT") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_9(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name not in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_10(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("XXfailXX", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_11(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("FAIL", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_12(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "XXraisesXX", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_13(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "RAISES", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_14(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "XXwarnsXX"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_15(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "WARNS"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_16(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return False
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_17(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id not in ("raises", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_18(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("XXraisesXX", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_19(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("RAISES", "warns"):
                    return True
    return False


def x__function_has_assertion__mutmut_20(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "XXwarnsXX"):
                    return True
    return False


def x__function_has_assertion__mutmut_21(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "WARNS"):
                    return True
    return False


def x__function_has_assertion__mutmut_22(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return False
    return False


def x__function_has_assertion__mutmut_23(node: ast.AST) -> bool:
    """True if a (async)FunctionDef body contains a substantive assertion.

    Recognises: bare `assert`, unittest `self.assertXxx(...)` / `self.fail()`,
    `pytest.raises`/`pytest.warns` (call or `with` context manager), numpy
    `np.testing.assert_*`, and bare `raises(...)`/`warns(...)` imports.
    """
    for sub in ast.walk(node):
        if isinstance(sub, ast.Assert):
            return True
        if isinstance(sub, ast.Call):
            fn = sub.func
            if isinstance(fn, ast.Attribute):
                name = fn.attr
                if name.startswith("assert") or name in ("fail", "raises", "warns"):
                    return True
            elif isinstance(fn, ast.Name):
                if fn.id in ("raises", "warns"):
                    return True
    return True

x__function_has_assertion__mutmut_mutants : ClassVar[MutantDict] = {
'x__function_has_assertion__mutmut_1': x__function_has_assertion__mutmut_1, 
    'x__function_has_assertion__mutmut_2': x__function_has_assertion__mutmut_2, 
    'x__function_has_assertion__mutmut_3': x__function_has_assertion__mutmut_3, 
    'x__function_has_assertion__mutmut_4': x__function_has_assertion__mutmut_4, 
    'x__function_has_assertion__mutmut_5': x__function_has_assertion__mutmut_5, 
    'x__function_has_assertion__mutmut_6': x__function_has_assertion__mutmut_6, 
    'x__function_has_assertion__mutmut_7': x__function_has_assertion__mutmut_7, 
    'x__function_has_assertion__mutmut_8': x__function_has_assertion__mutmut_8, 
    'x__function_has_assertion__mutmut_9': x__function_has_assertion__mutmut_9, 
    'x__function_has_assertion__mutmut_10': x__function_has_assertion__mutmut_10, 
    'x__function_has_assertion__mutmut_11': x__function_has_assertion__mutmut_11, 
    'x__function_has_assertion__mutmut_12': x__function_has_assertion__mutmut_12, 
    'x__function_has_assertion__mutmut_13': x__function_has_assertion__mutmut_13, 
    'x__function_has_assertion__mutmut_14': x__function_has_assertion__mutmut_14, 
    'x__function_has_assertion__mutmut_15': x__function_has_assertion__mutmut_15, 
    'x__function_has_assertion__mutmut_16': x__function_has_assertion__mutmut_16, 
    'x__function_has_assertion__mutmut_17': x__function_has_assertion__mutmut_17, 
    'x__function_has_assertion__mutmut_18': x__function_has_assertion__mutmut_18, 
    'x__function_has_assertion__mutmut_19': x__function_has_assertion__mutmut_19, 
    'x__function_has_assertion__mutmut_20': x__function_has_assertion__mutmut_20, 
    'x__function_has_assertion__mutmut_21': x__function_has_assertion__mutmut_21, 
    'x__function_has_assertion__mutmut_22': x__function_has_assertion__mutmut_22, 
    'x__function_has_assertion__mutmut_23': x__function_has_assertion__mutmut_23
}

def _function_has_assertion(*args, **kwargs):
    result = _mutmut_trampoline(x__function_has_assertion__mutmut_orig, x__function_has_assertion__mutmut_mutants, args, kwargs)
    return result 

_function_has_assertion.__signature__ = _mutmut_signature(x__function_has_assertion__mutmut_orig)
x__function_has_assertion__mutmut_orig.__name__ = 'x__function_has_assertion'


def x__run_ast_assertions__mutmut_orig(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_1(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = None
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_2(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(None)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_3(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = None
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_4(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 1
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_5(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = None
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_6(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 1
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_7(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = None

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_8(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = None
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_9(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = None
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_10(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root * rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_11(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_12(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            break
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_13(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob(None):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_14(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("XXtest_*.pyXX"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_15(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("TEST_*.PY"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_16(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = None
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_17(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(None)
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_18(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key not in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_19(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                break
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_20(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(None)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_21(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = None
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_22(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(None)
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_23(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding=None, errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_24(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors=None))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_25(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_26(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", ))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_27(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="XXutf-8XX", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_28(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="UTF-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_29(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="XXreplaceXX"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_30(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="REPLACE"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_31(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                break
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_32(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(None):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_33(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) or (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_34(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") and fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_35(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith(None) or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_36(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("XXtest_XX") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_37(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("TEST_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_38(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name != "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_39(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "XXtestXX"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_40(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "TEST"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_41(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total = 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_42(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total -= 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_43(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 2
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_44(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(None):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_45(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted = 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_46(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted -= 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_47(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 2
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_48(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(None)

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_49(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(None)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_50(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = None
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_51(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"XXtotalXX": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_52(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"TOTAL": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_53(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "XXassertedXX": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_54(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "ASSERTED": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_55(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "XXzero_assertXX": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_56(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "ZERO_ASSERT": zero_assert[:50]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_57(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:51]}
    return _json.dumps(summary), 0


def x__run_ast_assertions__mutmut_58(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(None), 0


def x__run_ast_assertions__mutmut_59(project_root: str) -> tuple[str, int]:
    """Scan test files and report assertion coverage of test functions.

    Returns (json_summary, 0) where summary = {total, asserted, zero_assert:[...]}.
    A test function with NO substantive assertion (a "pass-and-still-green" shell)
    is counted as zero_assert.  This is what test_assertion_quality means —
    pytest pass-rate cannot detect it.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    asserted = 0
    zero_assert: list[str] = []

    seen: set[str] = set()
    for rel in _TEST_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("test_*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            for fn in _ast.walk(tree):
                # pytest convention: `test_*` (or bare `test`). Avoid matching helpers
                # like `testing_*` / `tests_*` that are not test cases.
                if isinstance(fn, (_ast.FunctionDef, _ast.AsyncFunctionDef)) and (
                    fn.name.startswith("test_") or fn.name == "test"
                ):
                    total += 1
                    if _function_has_assertion(fn):
                        asserted += 1
                    else:
                        zero_assert.append(f"{path.relative_to(root)}::{fn.name}")

    summary = {"total": total, "asserted": asserted, "zero_assert": zero_assert[:50]}
    return _json.dumps(summary), 1

x__run_ast_assertions__mutmut_mutants : ClassVar[MutantDict] = {
'x__run_ast_assertions__mutmut_1': x__run_ast_assertions__mutmut_1, 
    'x__run_ast_assertions__mutmut_2': x__run_ast_assertions__mutmut_2, 
    'x__run_ast_assertions__mutmut_3': x__run_ast_assertions__mutmut_3, 
    'x__run_ast_assertions__mutmut_4': x__run_ast_assertions__mutmut_4, 
    'x__run_ast_assertions__mutmut_5': x__run_ast_assertions__mutmut_5, 
    'x__run_ast_assertions__mutmut_6': x__run_ast_assertions__mutmut_6, 
    'x__run_ast_assertions__mutmut_7': x__run_ast_assertions__mutmut_7, 
    'x__run_ast_assertions__mutmut_8': x__run_ast_assertions__mutmut_8, 
    'x__run_ast_assertions__mutmut_9': x__run_ast_assertions__mutmut_9, 
    'x__run_ast_assertions__mutmut_10': x__run_ast_assertions__mutmut_10, 
    'x__run_ast_assertions__mutmut_50': x__run_ast_assertions__mutmut_50, 
    'x__run_ast_assertions__mutmut_51': x__run_ast_assertions__mutmut_51, 
    'x__run_ast_assertions__mutmut_52': x__run_ast_assertions__mutmut_52, 
    'x__run_ast_assertions__mutmut_53': x__run_ast_assertions__mutmut_53, 
    'x__run_ast_assertions__mutmut_54': x__run_ast_assertions__mutmut_54, 
    'x__run_ast_assertions__mutmut_55': x__run_ast_assertions__mutmut_55, 
    'x__run_ast_assertions__mutmut_56': x__run_ast_assertions__mutmut_56, 
    'x__run_ast_assertions__mutmut_57': x__run_ast_assertions__mutmut_57, 
    'x__run_ast_assertions__mutmut_58': x__run_ast_assertions__mutmut_58, 
    'x__run_ast_assertions__mutmut_59': x__run_ast_assertions__mutmut_59
}

def _run_ast_assertions(*args, **kwargs):
    result = _mutmut_trampoline(x__run_ast_assertions__mutmut_orig, x__run_ast_assertions__mutmut_mutants, args, kwargs)
    return result 

_run_ast_assertions.__signature__ = _mutmut_signature(x__run_ast_assertions__mutmut_orig)
x__run_ast_assertions__mutmut_orig.__name__ = 'x__run_ast_assertions'


def x__run_ast_error_handling__mutmut_orig(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_1(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = None
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_2(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(None)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_3(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = None
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_4(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 1
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_5(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = None
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_6(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 1
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_7(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = None

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_8(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = None
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_9(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = None
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_10(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root * rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_11(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_12(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            break
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_13(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob(None):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_14(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("XX*.pyXX"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_15(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.PY"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_16(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = None
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_17(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(None)
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_18(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key not in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_19(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                break
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_20(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(None)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_21(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = None
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_22(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(None)
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_23(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding=None, errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_24(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors=None))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_25(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_26(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", ))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_27(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="XXutf-8XX", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_28(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="UTF-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_29(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="XXreplaceXX"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_30(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="REPLACE"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_31(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                break
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_32(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = None
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_33(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                None
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_34(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(None)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_35(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_36(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                break
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_37(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total = 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_38(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total -= 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_39(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 2
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_40(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = None
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_41(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                None
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_42(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) or n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_43(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(None)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_44(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler = 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_45(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler -= 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_46(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 2
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_47(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(None)

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_48(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(None))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_49(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(None)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_50(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = None
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_51(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"XXtotalXX": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_52(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"TOTAL": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_53(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "XXwith_handlerXX": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_54(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "WITH_HANDLER": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_55(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "XXno_handlerXX": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_56(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "NO_HANDLER": no_handler[:50]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_57(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:51]}
    return _json.dumps(summary), 0


def x__run_ast_error_handling__mutmut_58(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(None), 0


def x__run_ast_error_handling__mutmut_59(project_root: str) -> tuple[str, int]:
    """Scan source files and report file-level error-handling coverage.

    Returns (json_summary, 0) where summary = {total, with_handler, no_handler:[...]}.
    A source file counts as "handled" if it contains at least one try/except block
    with a real handler.  This is a framework-owned, independently reproducible
    measure of error_handling — it replaces the CRG flow path whose has_error_handler
    field does not exist in the installed code-review-graph package.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    total = 0
    with_handler = 0
    no_handler: list[str] = []

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
            # Skip files with no functions/classes (e.g. empty __init__.py) — they
            # have nothing to handle and would unfairly dilute the ratio.
            has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not has_code:
                continue
            total += 1
            handled = any(
                isinstance(n, _ast.Try) and n.handlers
                for n in _ast.walk(tree)
            )
            if handled:
                with_handler += 1
            else:
                no_handler.append(str(path.relative_to(root)))

    summary = {"total": total, "with_handler": with_handler, "no_handler": no_handler[:50]}
    return _json.dumps(summary), 1

x__run_ast_error_handling__mutmut_mutants : ClassVar[MutantDict] = {
'x__run_ast_error_handling__mutmut_1': x__run_ast_error_handling__mutmut_1, 
    'x__run_ast_error_handling__mutmut_2': x__run_ast_error_handling__mutmut_2, 
    'x__run_ast_error_handling__mutmut_3': x__run_ast_error_handling__mutmut_3, 
    'x__run_ast_error_handling__mutmut_4': x__run_ast_error_handling__mutmut_4, 
    'x__run_ast_error_handling__mutmut_5': x__run_ast_error_handling__mutmut_5, 
    'x__run_ast_error_handling__mutmut_6': x__run_ast_error_handling__mutmut_6, 
    'x__run_ast_error_handling__mutmut_7': x__run_ast_error_handling__mutmut_7, 
    'x__run_ast_error_handling__mutmut_8': x__run_ast_error_handling__mutmut_8, 
    'x__run_ast_error_handling__mutmut_9': x__run_ast_error_handling__mutmut_9, 
    'x__run_ast_error_handling__mutmut_10': x__run_ast_error_handling__mutmut_10, 
    'x__run_ast_error_handling__mutmut_11': x__run_ast_error_handling__mutmut_11, 
    'x__run_ast_error_handling__mutmut_12': x__run_ast_error_handling__mutmut_12, 
    'x__run_ast_error_handling__mutmut_13': x__run_ast_error_handling__mutmut_13, 
    'x__run_ast_error_handling__mutmut_14': x__run_ast_error_handling__mutmut_14, 
    'x__run_ast_error_handling__mutmut_15': x__run_ast_error_handling__mutmut_15, 
    'x__run_ast_error_handling__mutmut_16': x__run_ast_error_handling__mutmut_16, 
    'x__run_ast_error_handling__mutmut_17': x__run_ast_error_handling__mutmut_17, 
    'x__run_ast_error_handling__mutmut_18': x__run_ast_error_handling__mutmut_18, 
    'x__run_ast_error_handling__mutmut_19': x__run_ast_error_handling__mutmut_19, 
    'x__run_ast_error_handling__mutmut_20': x__run_ast_error_handling__mutmut_20, 
    'x__run_ast_error_handling__mutmut_21': x__run_ast_error_handling__mutmut_21, 
    'x__run_ast_error_handling__mutmut_22': x__run_ast_error_handling__mutmut_22, 
    'x__run_ast_error_handling__mutmut_23': x__run_ast_error_handling__mutmut_23, 
    'x__run_ast_error_handling__mutmut_24': x__run_ast_error_handling__mutmut_24, 
    'x__run_ast_error_handling__mutmut_25': x__run_ast_error_handling__mutmut_25, 
    'x__run_ast_error_handling__mutmut_26': x__run_ast_error_handling__mutmut_26, 
    'x__run_ast_error_handling__mutmut_27': x__run_ast_error_handling__mutmut_27, 
    'x__run_ast_error_handling__mutmut_28': x__run_ast_error_handling__mutmut_28, 
    'x__run_ast_error_handling__mutmut_29': x__run_ast_error_handling__mutmut_29, 
    'x__run_ast_error_handling__mutmut_30': x__run_ast_error_handling__mutmut_30, 
    'x__run_ast_error_handling__mutmut_31': x__run_ast_error_handling__mutmut_31, 
    'x__run_ast_error_handling__mutmut_32': x__run_ast_error_handling__mutmut_32, 
    'x__run_ast_error_handling__mutmut_33': x__run_ast_error_handling__mutmut_33, 
    'x__run_ast_error_handling__mutmut_34': x__run_ast_error_handling__mutmut_34, 
    'x__run_ast_error_handling__mutmut_35': x__run_ast_error_handling__mutmut_35, 
    'x__run_ast_error_handling__mutmut_36': x__run_ast_error_handling__mutmut_36, 
    'x__run_ast_error_handling__mutmut_37': x__run_ast_error_handling__mutmut_37, 
    'x__run_ast_error_handling__mutmut_38': x__run_ast_error_handling__mutmut_38, 
    'x__run_ast_error_handling__mutmut_39': x__run_ast_error_handling__mutmut_39, 
    'x__run_ast_error_handling__mutmut_40': x__run_ast_error_handling__mutmut_40, 
    'x__run_ast_error_handling__mutmut_41': x__run_ast_error_handling__mutmut_41, 
    'x__run_ast_error_handling__mutmut_42': x__run_ast_error_handling__mutmut_42, 
    'x__run_ast_error_handling__mutmut_43': x__run_ast_error_handling__mutmut_43, 
    'x__run_ast_error_handling__mutmut_44': x__run_ast_error_handling__mutmut_44, 
    'x__run_ast_error_handling__mutmut_45': x__run_ast_error_handling__mutmut_45, 
    'x__run_ast_error_handling__mutmut_46': x__run_ast_error_handling__mutmut_46, 
    'x__run_ast_error_handling__mutmut_47': x__run_ast_error_handling__mutmut_47, 
    'x__run_ast_error_handling__mutmut_48': x__run_ast_error_handling__mutmut_48, 
    'x__run_ast_error_handling__mutmut_49': x__run_ast_error_handling__mutmut_49, 
    'x__run_ast_error_handling__mutmut_50': x__run_ast_error_handling__mutmut_50, 
    'x__run_ast_error_handling__mutmut_51': x__run_ast_error_handling__mutmut_51, 
    'x__run_ast_error_handling__mutmut_52': x__run_ast_error_handling__mutmut_52, 
    'x__run_ast_error_handling__mutmut_53': x__run_ast_error_handling__mutmut_53, 
    'x__run_ast_error_handling__mutmut_54': x__run_ast_error_handling__mutmut_54, 
    'x__run_ast_error_handling__mutmut_55': x__run_ast_error_handling__mutmut_55, 
    'x__run_ast_error_handling__mutmut_56': x__run_ast_error_handling__mutmut_56, 
    'x__run_ast_error_handling__mutmut_57': x__run_ast_error_handling__mutmut_57, 
    'x__run_ast_error_handling__mutmut_58': x__run_ast_error_handling__mutmut_58, 
    'x__run_ast_error_handling__mutmut_59': x__run_ast_error_handling__mutmut_59
}

def _run_ast_error_handling(*args, **kwargs):
    result = _mutmut_trampoline(x__run_ast_error_handling__mutmut_orig, x__run_ast_error_handling__mutmut_mutants, args, kwargs)
    return result 

_run_ast_error_handling.__signature__ = _mutmut_signature(x__run_ast_error_handling__mutmut_orig)
x__run_ast_error_handling__mutmut_orig.__name__ = 'x__run_ast_error_handling'


def x__run_ast_docstrings__mutmut_orig(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_1(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = None
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_2(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(None)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_3(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = None
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_4(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = None
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_5(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 1
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_6(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = None
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_7(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 1
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_8(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = None
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_9(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_10(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith(None):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_11(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("XX_XX"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_12(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total = 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_13(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total -= 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_14(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 2
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_15(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(None):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_16(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc = 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_17(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc -= 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_18(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 2
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_19(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(None)
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_20(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_21(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith(None):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_22(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("XX_XX"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_23(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total = 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_24(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total -= 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_25(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 2
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_26(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(None):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_27(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc = 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_28(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc -= 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_29(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 2
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_30(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(None)

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_31(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_32(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith(None):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_33(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("XX_XX"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_34(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total = 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_35(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total -= 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_36(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 2
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_37(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(None):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_38(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc = 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_39(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc -= 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_40(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 2
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_41(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(None)

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_42(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(None)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_43(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = None
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_44(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 1
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_45(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = None
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_46(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 1
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_47(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = None
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_48(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = None

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_49(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = True

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_50(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = None
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_51(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = None
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_52(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root * rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_53(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_54(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            break
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_55(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob(None):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_56(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("XX*.pyXX"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_57(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.PY"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_58(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = None
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_59(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(None)
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_60(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key not in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_61(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                break
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_62(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(None)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_63(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = None
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_64(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(None)
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_65(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding=None, errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_66(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors=None))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_67(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_68(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", ))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_69(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="XXutf-8XX", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_70(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="UTF-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_71(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="XXreplaceXX"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_72(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="REPLACE"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_73(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                break
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_74(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = None
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_75(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                None
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_76(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(None)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_77(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_78(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                break
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_79(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = None

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_80(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = False

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_81(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = None
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_82(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(None)
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_83(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(None))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_84(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(None)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_85(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total = visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_86(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total -= visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_87(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc = visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_88(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc -= visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_89(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(None)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_90(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_91(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps(None), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_92(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 1

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_93(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = None
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_94(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"XXtotalXX": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_95(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"TOTAL": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_96(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "XXwith_docXX": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_97(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "WITH_DOC": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_98(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "XXmissingXX": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_99(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "MISSING": missing[:50]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_100(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:51]}
    return _json.dumps(summary), 0


def x__run_ast_docstrings__mutmut_101(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(None), 0


def x__run_ast_docstrings__mutmut_102(project_root: str) -> tuple[str, int]:
    """Scan source files and report public-API docstring coverage.

    Returns (json_summary, 0) where summary = {total, with_doc, missing:[...]}.
    A "public" def/class is one whose name does not start with '_' (excludes
    private members and dunders like __init__). This is a framework-owned,
    independently reproducible documentation measure — it replaces pydocstyle's
    style-only check with actual docstring presence on the public surface.
    """
    import ast as _ast
    import json as _json
    from pathlib import Path as _Path

    root = _Path(project_root)
    
    class PublicAPIVisitor(_ast.NodeVisitor):
        def __init__(self, filepath):
            self.filepath = filepath
            self.total = 0
            self.with_doc = 0
            self.missing = []
            
        def visit_ClassDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")
                # Visit methods, but do not generic_visit to avoid nested classes
                for child in node.body:
                    if isinstance(child, (_ast.FunctionDef, _ast.AsyncFunctionDef)):
                        if not child.name.startswith("_"):
                            self.total += 1
                            if _ast.get_docstring(child):
                                self.with_doc += 1
                            else:
                                self.missing.append(f"{self.filepath}::{node.name}.{child.name}")

        def visit_FunctionDef(self, node):
            if not node.name.startswith("_"):
                self.total += 1
                if _ast.get_docstring(node):
                    self.with_doc += 1
                else:
                    self.missing.append(f"{self.filepath}::{node.name}")

        def visit_AsyncFunctionDef(self, node):
            self.visit_FunctionDef(node)

    total = 0
    with_doc = 0
    missing: list[str] = []
    has_code = False

    seen: set[str] = set()
    for rel in _SRC_DIRS:
        base = root / rel
        if not base.is_dir():
            continue
        for path in base.rglob("*.py"):
            key = str(path.resolve())
            if key in seen:
                continue
            seen.add(key)
            try:
                tree = _ast.parse(path.read_text(encoding="utf-8", errors="replace"))
            except (SyntaxError, ValueError, OSError):
                continue
                
            # Skip files with no functions/classes
            file_has_code = any(
                isinstance(n, (_ast.FunctionDef, _ast.AsyncFunctionDef, _ast.ClassDef))
                for n in _ast.walk(tree)
            )
            if not file_has_code:
                continue
            
            has_code = True

            visitor = PublicAPIVisitor(path.relative_to(root))
            visitor.visit(tree)
            total += visitor.total
            with_doc += visitor.with_doc
            missing.extend(visitor.missing)

    if not has_code:
        return _json.dumps({}), 0

    summary = {"total": total, "with_doc": with_doc, "missing": missing[:50]}
    return _json.dumps(summary), 1

x__run_ast_docstrings__mutmut_mutants : ClassVar[MutantDict] = {
'x__run
0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_2(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 1:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_3(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = None
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_4(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "XXruffXX":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_5(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "RUFF":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_6(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "XXmypyXX":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_7(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "MYPY":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_8(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "XXpyrightXX":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_9(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "PYRIGHT":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_10(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "XXpytest-covXX":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_11(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "PYTEST-COV":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_12(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: None,
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_13(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(None, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_14(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=None),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_15(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_16(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, ),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_17(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=False),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_18(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "XXpytestXX":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_19(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "PYTEST":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_20(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: None,
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_21(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(None, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_22(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=None),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_23(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_24(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, ),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_25(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=True),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_26(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "XXgitleaksXX":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_27(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "GITLEAKS":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_28(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "XXbanditXX":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_29(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "BANDIT":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_30(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "XXradon-ccXX":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_31(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "RADON-CC":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_32(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "XXradon-miXX":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_33(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "RADON-MI":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_34(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "XXpytest-benchmarkXX": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_35(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "PYTEST-BENCHMARK": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_36(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "XXast-assertionsXX":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_37(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "AST-ASSERTIONS":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_38(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "XXast-error-handlingXX": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_39(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "AST-ERROR-HANDLING": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_40(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "XXast-docstringsXX":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_41(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "AST-DOCSTRINGS":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_42(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "XXpytest-cov-integrationXX": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_43(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "PYTEST-COV-INTEGRATION": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_44(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: None,
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_45(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(None, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_46(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=None),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_47(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_48(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, ),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_49(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=False),
    }
    fn = scorers.get(tool)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_50(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = None
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_51(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(None)
    return fn(output, returncode) if fn else None


def x_compute_tool_score__mutmut_52(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(None, returncode) if fn else None


def x_compute_tool_score__mutmut_53(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, None) if fn else None


def x_compute_tool_score__mutmut_54(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(returncode) if fn else None


def x_compute_tool_score__mutmut_55(tool: str, output: str, returncode: int) -> Optional[float]:
    """Compute a 0-100 score from *tool* output.

    Returns None when the score cannot be determined (tool skipped / timed out /
    not found / unknown tool).
    """
    if returncode < 0:
        return None  # Harness-internal codes — cannot score

    scorers = {
        "ruff":             _score_ruff,
        "mypy":             _score_mypy,
        "pyright":          _score_pyright,
        "pytest-cov":       lambda o, _rc: _score_pytest(o, coverage=True),
        "pytest":           lambda o, _rc: _score_pytest(o, coverage=False),
        "gitleaks":         _score_gitleaks,
        "bandit":           _score_bandit,
        "radon-cc":         _score_radon_cc,
        "radon-mi":         _score_radon_mi,
        "pytest-benchmark": _score_pytest_benchmark,
        "ast-assertions":   _score_assertion_quality,
        "ast-error-handling": _score_error_handling_coverage,
        "ast-docstrings":   _score_docstring_coverage,
        "pytest-cov-integration": lambda o, _rc: _score_pytest(o, coverage=True),
    }
    fn = scorers.get(tool)
    return fn(output, ) if fn else None

x_compute_tool_score__mutmut_mutants : ClassVar[MutantDict] = {
'x_compute_tool_score__mutmut_1': x_compute_tool_score__mutmut_1, 
    'x_compute_tool_score__mutmut_2': x_compute_tool_score__mutmut_2, 
    'x_compute_tool_score__mutmut_3': x_compute_tool_score__mutmut_3, 
    'x_compute_tool_score__mutmut_4': x_compute_tool_score__mutmut_4, 
    'x_compute_tool_score__mutmut_5': x_compute_tool_score__mutmut_5, 
    'x_compute_tool_score__mutmut_6': x_compute_tool_score__mutmut_6, 
    'x_compute_tool_score__mutmut_7': x_compute_tool_score__mutmut_7, 
    'x_compute_tool_score__mutmut_8': x_compute_tool_score__mutmut_8, 
    'x_compute_tool_score__mutmut_9': x_compute_tool_score__mutmut_9, 
    'x_compute_tool_score__mutmut_10': x_compute_tool_score__mutmut_10, 
    'x_compute_tool_score__mutmut_11': x_compute_tool_score__mutmut_11, 
    'x_compute_tool_score__mutmut_12': x_compute_tool_score__mutmut_12, 
    'x_compute_tool_score__mutmut_13': x_compute_tool_score__mutmut_13, 
    'x_compute_tool_score__mutmut_14': x_compute_tool_score__mutmut_14, 
    'x_compute_tool_score__mutmut_15': x_compute_tool_score__mutmut_15, 
    'x_compute_tool_score__mutmut_16': x_compute_tool_score__mutmut_16, 
    'x_compute_tool_score__mutmut_17': x_compute_tool_score__mutmut_17, 
    'x_compute_tool_score__mutmut_18': x_compute_tool_score__mutmut_18, 
    'x_compute_tool_score__mutmut_19': x_compute_tool_score__mutmut_19, 
    'x_compute_tool_score__mutmut_20': x_compute_tool_score__mutmut_20, 
    'x_compute_tool_score__mutmut_21': x_compute_tool_score__mutmut_21, 
    'x_compute_tool_score__mutmut_22': x_compute_tool_score__mutmut_22, 
    'x_compute_tool_score__mutmut_23': x_compute_tool_score__mutmut_23, 
    'x_compute_tool_score__mutmut_24': x_compute_tool_score__mutmut_24, 
    'x_compute_tool_score__mutmut_25': x_compute_tool_score__mutmut_25, 
    'x_compute_tool_score__mutmut_26': x_compute_tool_score__mutmut_26, 
    'x_compute_tool_score__mutmut_27': x_compute_tool_score__mutmut_27, 
    'x_compute_tool_score__mutmut_28': x_compute_tool_score__mutmut_28, 
    'x_compute_tool_score__mutmut_29': x_compute_tool_score__mutmut_29, 
    'x_compute_tool_score__mutmut_30': x_compute_tool_score__mutmut_30, 
    'x_compute_tool_score__mutmut_31': x_compute_tool_score__mutmut_31, 
    'x_compute_tool_score__mutmut_32': x_compute_tool_score__mutmut_32, 
    'x_compute_tool_score__mutmut_33': x_compute_tool_score__mutmut_33, 
    'x_compute_tool_score__mutmut_34': x_compute_tool_score__mutmut_34, 
    'x_compute_tool_score__mutmut_35': x_compute_tool_score__mutmut_35, 
    'x_compute_tool_score__mutmut_36': x_compute_tool_score__mutmut_36, 
    'x_compute_tool_score__mutmut_37': x_compute_tool_score__mutmut_37, 
    'x_compute_tool_score__mutmut_38': x_compute_tool_score__mutmut_38, 
    'x_compute_tool_score__mutmut_39': x_compute_tool_score__mutmut_39, 
    'x_compute_tool_score__mutmut_40': x_compute_tool_score__mutmut_40, 
    'x_compute_tool_score__mutmut_41': x_compute_tool_score__mutmut_41, 
    'x_compute_tool_score__mutmut_42': x_compute_tool_score__mutmut_42, 
    'x_compute_tool_score__mutmut_43': x_compute_tool_score__mutmut_43, 
    'x_compute_tool_score__mutmut_44': x_compute_tool_score__mutmut_44, 
    'x_compute_tool_score__mutmut_45': x_compute_tool_score__mutmut_45, 
    'x_compute_tool_score__mutmut_46': x_compute_tool_score__mutmut_46, 
    'x_compute_tool_score__mutmut_47': x_compute_tool_score__mutmut_47, 
    'x_compute_tool_score__mutmut_48': x_compute_tool_score__mutmut_48, 
    'x_compute_tool_score__mutmut_49': x_compute_tool_score__mutmut_49, 
    'x_compute_tool_score__mutmut_50': x_compute_tool_score__mutmut_50, 
    'x_compute_tool_score__mutmut_51': x_compute_tool_score__mutmut_51, 
    'x_compute_tool_score__mutmut_52': x_compute_tool_score__mutmut_52, 
    'x_compute_tool_score__mutmut_53': x_compute_tool_score__mutmut_53, 
    'x_compute_tool_score__mutmut_54': x_compute_tool_score__mutmut_54, 
    'x_compute_tool_score__mutmut_55': x_compute_tool_score__mutmut_55
}

def compute_tool_score(*args, **kwargs):
    result = _mutmut_trampoline(x_compute_tool_score__mutmut_orig, x_compute_tool_score__mutmut_mutants, args, kwargs)
    return result 

compute_tool_score.__signature__ = _mutmut_signature(x_compute_tool_score__mutmut_orig)
x_compute_tool_score__mutmut_orig.__name__ = 'x_compute_tool_score'


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_orig(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_1(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = None
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_2(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(None)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_3(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = None
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_4(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 1
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_5(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = None

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_6(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count != 0:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_7(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 1:
        return 100.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_8(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 101.0
    return max(0.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_9(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(None, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_10(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, None)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_11(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_12(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, )


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_13(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(1.0, 100.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_14(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 + count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_15(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 101.0 - count * 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_16(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count / 2.0)


# ---------------------------------------------------------------------------
# Per-tool scoring helpers
# ---------------------------------------------------------------------------

def x__score_ruff__mutmut_17(output: str, _returncode: int) -> float:
    """Score ruff JSON output.  0 violations → 100; each violation costs 2 pts."""
    import json as _json
    try:
        violations = _json.loads(output)
        count = len(violations) if isinstance(violations, list) else 0
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format lines (file:line:col:)
        count = len(re.findall(r"^\S+\.(py|pyi):\d+:\d+:", output, re.MULTILINE))

    if count == 0:
        return 100.0
    return max(0.0, 100.0 - count * 3.0)

x__score_ruff__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_ruff__mutmut_1': x__score_ruff__mutmut_1, 
    'x__score_ruff__mutmut_2': x__score_ruff__mutmut_2, 
    'x__score_ruff__mutmut_3': x__score_ruff__mutmut_3, 
    'x__score_ruff__mutmut_4': x__score_ruff__mutmut_4, 
    'x__score_ruff__mutmut_5': x__score_ruff__mutmut_5, 
    'x__score_ruff__mutmut_6': x__score_ruff__mutmut_6, 
    'x__score_ruff__mutmut_7': x__score_ruff__mutmut_7, 
    'x__score_ruff__mutmut_8': x__score_ruff__mutmut_8, 
    'x__score_ruff__mutmut_9': x__score_ruff__mutmut_9, 
    'x__score_ruff__mutmut_10': x__score_ruff__mutmut_10, 
    'x__score_ruff__mutmut_11': x__score_ruff__mutmut_11, 
    'x__score_ruff__mutmut_12': x__score_ruff__mutmut_12, 
    'x__score_ruff__mutmut_13': x__score_ruff__mutmut_13, 
    'x__score_ruff__mutmut_14': x__score_ruff__mutmut_14, 
    'x__score_ruff__mutmut_15': x__score_ruff__mutmut_15, 
    'x__score_ruff__mutmut_16': x__score_ruff__mutmut_16, 
    'x__score_ruff__mutmut_17': x__score_ruff__mutmut_17
}

def _score_ruff(*args, **kwargs):
    result = _mutmut_trampoline(x__score_ruff__mutmut_orig, x__score_ruff__mutmut_mutants, args, kwargs)
    return result 

_score_ruff.__signature__ = _mutmut_signature(x__score_ruff__mutmut_orig)
x__score_ruff__mutmut_orig.__name__ = 'x__score_ruff'


def x__score_mypy__mutmut_orig(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_1(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "XXSuccess: no issues foundXX" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_2(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_3(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "SUCCESS: NO ISSUES FOUND" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_4(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" not in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_5(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 101.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_6(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = None
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_7(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors != 0:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_8(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 1:
        return 100.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_9(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 101.0
    return max(0.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_10(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(None, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_11(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, None)


def x__score_mypy__mutmut_12(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(100.0 - errors * 5.0)


def x__score_mypy__mutmut_13(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, )


def x__score_mypy__mutmut_14(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(1.0, 100.0 - errors * 5.0)


def x__score_mypy__mutmut_15(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 + errors * 5.0)


def x__score_mypy__mutmut_16(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 101.0 - errors * 5.0)


def x__score_mypy__mutmut_17(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors / 5.0)


def x__score_mypy__mutmut_18(output: str, _returncode: int) -> float:
    """Score mypy text output.  0 errors → 100; each error costs 5 pts."""
    if "Success: no issues found" in output:
        return 100.0
    errors = len(re.findall(r":\s*error:", output))
    if errors == 0:
        return 100.0
    return max(0.0, 100.0 - errors * 6.0)

x__score_mypy__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_mypy__mutmut_1': x__score_mypy__mutmut_1, 
    'x__score_mypy__mutmut_2': x__score_mypy__mutmut_2, 
    'x__score_mypy__mutmut_3': x__score_mypy__mutmut_3, 
    'x__score_mypy__mutmut_4': x__score_mypy__mutmut_4, 
    'x__score_mypy__mutmut_5': x__score_mypy__mutmut_5, 
    'x__score_mypy__mutmut_6': x__score_mypy__mutmut_6, 
    'x__score_mypy__mutmut_7': x__score_mypy__mutmut_7, 
    'x__score_mypy__mutmut_8': x__score_mypy__mutmut_8, 
    'x__score_mypy__mutmut_9': x__score_mypy__mutmut_9, 
    'x__score_mypy__mutmut_10': x__score_mypy__mutmut_10, 
    'x__score_mypy__mutmut_11': x__score_mypy__mutmut_11, 
    'x__score_mypy__mutmut_12': x__score_mypy__mutmut_12, 
    'x__score_mypy__mutmut_13': x__score_mypy__mutmut_13, 
    'x__score_mypy__mutmut_14': x__score_mypy__mutmut_14, 
    'x__score_mypy__mutmut_15': x__score_mypy__mutmut_15, 
    'x__score_mypy__mutmut_16': x__score_mypy__mutmut_16, 
    'x__score_mypy__mutmut_17': x__score_mypy__mutmut_17, 
    'x__score_mypy__mutmut_18': x__score_mypy__mutmut_18
}

def _score_mypy(*args, **kwargs):
    result = _mutmut_trampoline(x__score_mypy__mutmut_orig, x__score_mypy__mutmut_mutants, args, kwargs)
    return result 

_score_mypy.__signature__ = _mutmut_signature(x__score_mypy__mutmut_orig)
x__score_mypy__mutmut_orig.__name__ = 'x__score_mypy'


def x__score_pytest__mutmut_orig(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_1(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = None
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_2(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(None, output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_3(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", None)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_4(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_5(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", )
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_6(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"XXTOTAL\s+(?:\d+\s+){2,}(\d+)%XX", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_7(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"total\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_8(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_9(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(None)
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_10(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(None))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_11(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(2))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_12(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = None
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_13(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(None, output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_14(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", None)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_15(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_16(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", )
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_17(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"XX(\d+) passedXX", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_18(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_19(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) PASSED", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_20(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = None
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_21(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(None, output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_22(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", None)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_23(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_24(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", )
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_25(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"XX(\d+) failedXX", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_26(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_27(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) FAILED", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_28(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = None
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_29(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(None) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_30(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(None)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_31(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(2)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_32(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 1
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_33(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = None
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_34(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(None) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_35(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(None)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_36(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(2)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_37(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 1
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_38(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = None
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_39(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed - failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_40(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total != 0:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_41(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 1:
        return 0.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_42(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 1.0
    return round(100.0 * passed / total, 1)


def x__score_pytest__mutmut_43(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(None, 1)


def x__score_pytest__mutmut_44(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, None)


def x__score_pytest__mutmut_45(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(1)


def x__score_pytest__mutmut_46(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, )


def x__score_pytest__mutmut_47(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed * total, 1)


def x__score_pytest__mutmut_48(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 / passed / total, 1)


def x__score_pytest__mutmut_49(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(101.0 * passed / total, 1)


def x__score_pytest__mutmut_50(output: str, *, coverage: bool) -> float:
    """Score pytest (and pytest-cov) output.

    With coverage=True: return total line-coverage percentage.
    With coverage=False: return 100 × (passed / total).
    """
    if coverage:
        m = re.search(r"TOTAL\s+(?:\d+\s+){2,}(\d+)%", output)
        if m:
            return float(m.group(1))
        # Fallback to pass-rate if coverage table absent
    passed_m = re.search(r"(\d+) passed", output)
    failed_m = re.search(r"(\d+) failed", output)
    passed = int(passed_m.group(1)) if passed_m else 0
    failed = int(failed_m.group(1)) if failed_m else 0
    total = passed + failed
    if total == 0:
        return 0.0
    return round(100.0 * passed / total, 2)

x__score_pytest__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_pytest__mutmut_1': x__score_pytest__mutmut_1, 
    'x__score_pytest__mutmut_2': x__score_pytest__mutmut_2, 
    'x__score_pytest__mutmut_3': x__score_pytest__mutmut_3, 
    'x__score_pytest__mutmut_4': x__score_pytest__mutmut_4, 
    'x__score_pytest__mutmut_5': x__score_pytest__mutmut_5, 
    'x__score_pytest__mutmut_6': x__score_pytest__mutmut_6, 
    'x__score_pytest__mutmut_7': x__score_pytest__mutmut_7, 
    'x__score_pytest__mutmut_8': x__score_pytest__mutmut_8, 
    'x__score_pytest__mutmut_9': x__score_pytest__mutmut_9, 
    'x__score_pytest__mutmut_10': x__score_pytest__mutmut_10, 
    'x__score_pytest__mutmut_11': x__score_pytest__mutmut_11, 
    'x__score_pytest__mutmut_12': x__score_pytest__mutmut_12, 
    'x__score_pytest__mutmut_13': x__score_pytest__mutmut_13, 
    'x__score_pytest__mutmut_14': x__score_pytest__mutmut_14, 
    'x__score_pytest__mutmut_15': x__score_pytest__mutmut_15, 
    'x__score_pytest__mutmut_16': x__score_pytest__mutmut_16, 
    'x__score_pytest__mutmut_17': x__score_pytest__mutmut_17, 
    'x__score_pytest__mutmut_18': x__score_pytest__mutmut_18, 
    'x__score_pytest__mutmut_19': x__score_pytest__mutmut_19, 
    'x__score_pytest__mutmut_20': x__score_pytest__mutmut_20, 
    'x__score_pytest__mutmut_21': x__score_pytest__mutmut_21, 
    'x__score_pytest__mutmut_22': x__score_pytest__mutmut_22, 
    'x__score_pytest__mutmut_23': x__score_pytest__mutmut_23, 
    'x__score_pytest__mutmut_24': x__score_pytest__mutmut_24, 
    'x__score_pytest__mutmut_25': x__score_pytest__mutmut_25, 
    'x__score_pytest__mutmut_26': x__score_pytest__mutmut_26, 
    'x__score_pytest__mutmut_27': x__score_pytest__mutmut_27, 
    'x__score_pytest__mutmut_28': x__score_pytest__mutmut_28, 
    'x__score_pytest__mutmut_29': x__score_pytest__mutmut_29, 
    'x__score_pytest__mutmut_30': x__score_pytest__mutmut_30, 
    'x__score_pytest__mutmut_31': x__score_pytest__mutmut_31, 
    'x__score_pytest__mutmut_32': x__score_pytest__mutmut_32, 
    'x__score_pytest__mutmut_33': x__score_pytest__mutmut_33, 
    'x__score_pytest__mutmut_34': x__score_pytest__mutmut_34, 
    'x__score_pytest__mutmut_35': x__score_pytest__mutmut_35, 
    'x__score_pytest__mutmut_36': x__score_pytest__mutmut_36, 
    'x__score_pytest__mutmut_37': x__score_pytest__mutmut_37, 
    'x__score_pytest__mutmut_38': x__score_pytest__mutmut_38, 
    'x__score_pytest__mutmut_39': x__score_pytest__mutmut_39, 
    'x__score_pytest__mutmut_40': x__score_pytest__mutmut_40, 
    'x__score_pytest__mutmut_41': x__score_pytest__mutmut_41, 
    'x__score_pytest__mutmut_42': x__score_pytest__mutmut_42, 
    'x__score_pytest__mutmut_43': x__score_pytest__mutmut_43, 
    'x__score_pytest__mutmut_44': x__score_pytest__mutmut_44, 
    'x__score_pytest__mutmut_45': x__score_pytest__mutmut_45, 
    'x__score_pytest__mutmut_46': x__score_pytest__mutmut_46, 
    'x__score_pytest__mutmut_47': x__score_pytest__mutmut_47, 
    'x__score_pytest__mutmut_48': x__score_pytest__mutmut_48, 
    'x__score_pytest__mutmut_49': x__score_pytest__mutmut_49, 
    'x__score_pytest__mutmut_50': x__score_pytest__mutmut_50
}

def _score_pytest(*args, **kwargs):
    result = _mutmut_trampoline(x__score_pytest__mutmut_orig, x__score_pytest__mutmut_mutants, args, kwargs)
    return result 

_score_pytest.__signature__ = _mutmut_signature(x__score_pytest__mutmut_orig)
x__score_pytest__mutmut_orig.__name__ = 'x__score_pytest'


def x__score_gitleaks__mutmut_orig(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" in output or returncode == 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_1(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" in output and returncode == 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_2(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "XXNo leaks foundXX" in output or returncode == 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_3(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "no leaks found" in output or returncode == 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_4(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "NO LEAKS FOUND" in output or returncode == 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_5(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" not in output or returncode == 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_6(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" in output or returncode != 0:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_7(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" in output or returncode == 1:
        return 100.0
    return 0.0


def x__score_gitleaks__mutmut_8(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" in output or returncode == 0:
        return 101.0
    return 0.0


def x__score_gitleaks__mutmut_9(output: str, returncode: int) -> float:
    """Score gitleaks output.  No leaks → 100; any leaks → 0."""
    if "No leaks found" in output or returncode == 0:
        return 100.0
    return 1.0

x__score_gitleaks__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_gitleaks__mutmut_1': x__score_gitleaks__mutmut_1, 
    'x__score_gitleaks__mutmut_2': x__score_gitleaks__mutmut_2, 
    'x__score_gitleaks__mutmut_3': x__score_gitleaks__mutmut_3, 
    'x__score_gitleaks__mutmut_4': x__score_gitleaks__mutmut_4, 
    'x__score_gitleaks__mutmut_5': x__score_gitleaks__mutmut_5, 
    'x__score_gitleaks__mutmut_6': x__score_gitleaks__mutmut_6, 
    'x__score_gitleaks__mutmut_7': x__score_gitleaks__mutmut_7, 
    'x__score_gitleaks__mutmut_8': x__score_gitleaks__mutmut_8, 
    'x__score_gitleaks__mutmut_9': x__score_gitleaks__mutmut_9
}

def _score_gitleaks(*args, **kwargs):
    result = _mutmut_trampoline(x__score_gitleaks__mutmut_orig, x__score_gitleaks__mutmut_mutants, args, kwargs)
    return result 

_score_gitleaks.__signature__ = _mutmut_signature(x__score_gitleaks__mutmut_orig)
x__score_gitleaks__mutmut_orig.__name__ = 'x__score_gitleaks'


def x__score_pyright__mutmut_orig(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_1(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = None
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_2(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(None)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_3(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = None
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_4(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get(None, 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_5(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", None)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_6(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get(0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_7(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", )
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_8(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get(None, {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_9(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", None).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_10(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get({}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_11(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", ).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_12(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("XXsummaryXX", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_13(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("SUMMARY", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_14(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("XXerrorCountXX", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_15(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorcount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_16(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("ERRORCOUNT", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_17(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 1)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_18(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = None
    return max(0.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_19(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(None, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_20(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, None)


def x__score_pyright__mutmut_21(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(100.0 - errors * 5.0)


def x__score_pyright__mutmut_22(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, )


def x__score_pyright__mutmut_23(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(1.0, 100.0 - errors * 5.0)


def x__score_pyright__mutmut_24(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 + errors * 5.0)


def x__score_pyright__mutmut_25(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 101.0 - errors * 5.0)


def x__score_pyright__mutmut_26(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors / 5.0)


def x__score_pyright__mutmut_27(output: str, _returncode: int) -> float:
    """Score pyright --outputjson.  0 errors → 100; each error costs 5 pts."""
    import json as _json
    try:
        data = _json.loads(output)
        errors = data.get("summary", {}).get("errorCount", 0)
    except (_json.JSONDecodeError, ValueError):
        # Fall back to counting text-format "error:" lines.
        errors = len(re.findall(r"\berror:", output))
    return max(0.0, 100.0 - errors * 6.0)

x__score_pyright__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_pyright__mutmut_1': x__score_pyright__mutmut_1, 
    'x__score_pyright__mutmut_2': x__score_pyright__mutmut_2, 
    'x__score_pyright__mutmut_3': x__score_pyright__mutmut_3, 
    'x__score_pyright__mutmut_4': x__score_pyright__mutmut_4, 
    'x__score_pyright__mutmut_5': x__score_pyright__mutmut_5, 
    'x__score_pyright__mutmut_6': x__score_pyright__mutmut_6, 
    'x__score_pyright__mutmut_7': x__score_pyright__mutmut_7, 
    'x__score_pyright__mutmut_8': x__score_pyright__mutmut_8, 
    'x__score_pyright__mutmut_9': x__score_pyright__mutmut_9, 
    'x__score_pyright__mutmut_10': x__score_pyright__mutmut_10, 
    'x__score_pyright__mutmut_11': x__score_pyright__mutmut_11, 
    'x__score_pyright__mutmut_12': x__score_pyright__mutmut_12, 
    'x__score_pyright__mutmut_13': x__score_pyright__mutmut_13, 
    'x__score_pyright__mutmut_14': x__score_pyright__mutmut_14, 
    'x__score_pyright__mutmut_15': x__score_pyright__mutmut_15, 
    'x__score_pyright__mutmut_16': x__score_pyright__mutmut_16, 
    'x__score_pyright__mutmut_17': x__score_pyright__mutmut_17, 
    'x__score_pyright__mutmut_18': x__score_pyright__mutmut_18, 
    'x__score_pyright__mutmut_19': x__score_pyright__mutmut_19, 
    'x__score_pyright__mutmut_20': x__score_pyright__mutmut_20, 
    'x__score_pyright__mutmut_21': x__score_pyright__mutmut_21, 
    'x__score_pyright__mutmut_22': x__score_pyright__mutmut_22, 
    'x__score_pyright__mutmut_23': x__score_pyright__mutmut_23, 
    'x__score_pyright__mutmut_24': x__score_pyright__mutmut_24, 
    'x__score_pyright__mutmut_25': x__score_pyright__mutmut_25, 
    'x__score_pyright__mutmut_26': x__score_pyright__mutmut_26, 
    'x__score_pyright__mutmut_27': x__score_pyright__mutmut_27
}

def _score_pyright(*args, **kwargs):
    result = _mutmut_trampoline(x__score_pyright__mutmut_orig, x__score_pyright__mutmut_mutants, args, kwargs)
    return result 

_score_pyright.__signature__ = _mutmut_signature(x__score_pyright__mutmut_orig)
x__score_pyright__mutmut_orig.__name__ = 'x__score_pyright'


def x__score_bandit__mutmut_orig(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_1(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = None
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_2(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(None)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_3(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = None
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_4(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get(None, [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_5(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", None)
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_6(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get([])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_7(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", )
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_8(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("XXresultsXX", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_9(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("RESULTS", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_10(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = None
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_11(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(None)
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_12(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(2 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_13(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get(None) == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_14(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("XXissue_severityXX") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_15(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("ISSUE_SEVERITY") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_16(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") != "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_17(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "XXHIGHXX")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_18(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "high")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_19(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = None
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_20(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(None)
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_21(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(2 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_22(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get(None) == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_23(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("XXissue_severityXX") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_24(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("ISSUE_SEVERITY") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_25(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") != "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_26(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "XXMEDIUMXX")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_27(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "medium")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_28(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = None
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_29(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(None)
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_30(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(2 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_31(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get(None) == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_32(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("XXissue_severityXX") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_33(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("ISSUE_SEVERITY") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_34(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") != "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_35(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "XXLOWXX")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_36(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "low")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_37(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(None, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_38(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, None)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_39(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_40(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, )
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_41(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(1.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_42(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 + low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_43(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 + medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_44(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 + high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_45(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 101.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_46(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high / 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_47(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 11.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_48(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium / 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_49(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 4.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_50(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low / 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_51(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 2.0)
    except (_json.JSONDecodeError, ValueError):
        return 0.0


def x__score_bandit__mutmut_52(output: str, _returncode: int) -> float:
    """Score bandit -f json.  HIGH=−10, MEDIUM=−3, LOW=−1 per issue."""
    import json as _json
    try:
        data = _json.loads(output)
        results = data.get("results", [])
        high   = sum(1 for r in results if r.get("issue_severity") == "HIGH")
        medium = sum(1 for r in results if r.get("issue_severity") == "MEDIUM")
        low    = sum(1 for r in results if r.get("issue_severity") == "LOW")
        return max(0.0, 100.0 - high * 10.0 - medium * 3.0 - low * 1.0)
    except (_json.JSONDecodeError, ValueError):
        return 1.0

x__score_bandit__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_bandit__mutmut_1': x__score_bandit__mutmut_1, 
    'x__score_bandit__mutmut_2': x__score_bandit__mutmut_2, 
    'x__score_bandit__mutmut_3': x__score_bandit__mutmut_3, 
    'x__score_bandit__mutmut_4': x__score_bandit__mutmut_4, 
    'x__score_bandit__mutmut_5': x__score_bandit__mutmut_5, 
    'x__score_bandit__mutmut_6': x__score_bandit__mutmut_6, 
    'x__score_bandit__mutmut_7': x__score_bandit__mutmut_7, 
    'x__score_bandit__mutmut_8': x__score_bandit__mutmut_8, 
output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_1(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = None
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_2(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(None)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_3(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = None
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_4(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            None
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_5(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            2
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_6(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) or entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_7(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get(None, 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_8(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", None) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_9(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get(0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_10(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", ) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_11(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("XXcomplexityXX", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_12(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("COMPLEXITY", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_13(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 1) > 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_14(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) >= 10
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_15(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 11
        )
        return max(0.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_16(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(None, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_17(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, None)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_18(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_19(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, )
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_20(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(1.0, 100.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_21(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 + complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_22(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 101.0 - complex_count * 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_23(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count / 5.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_cc__mutmut_24(output: str, _returncode: int) -> Optional[float]:
    """Score radon cc -j.  Functions with CC > 10 (grade C+) each cost 5 pts.

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon cc -j: {"file.py": [{"complexity": N, "type": "function", ...}, ...]}
        complex_count = sum(
            1
            for entries in data.values() if isinstance(entries, list)
            for entry in entries
            if isinstance(entry, dict) and entry.get("complexity", 0) > 10
        )
        return max(0.0, 100.0 - complex_count * 6.0)
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score

x__score_radon_cc__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_radon_cc__mutmut_1': x__score_radon_cc__mutmut_1, 
    'x__score_radon_cc__mutmut_2': x__score_radon_cc__mutmut_2, 
    'x__score_radon_cc__mutmut_3': x__score_radon_cc__mutmut_3, 
    'x__score_radon_cc__mutmut_4': x__score_radon_cc__mutmut_4, 
    'x__score_radon_cc__mutmut_5': x__score_radon_cc__mutmut_5, 
    'x__score_radon_cc__mutmut_6': x__score_radon_cc__mutmut_6, 
    'x__score_radon_cc__mutmut_7': x__score_radon_cc__mutmut_7, 
    'x__score_radon_cc__mutmut_8': x__score_radon_cc__mutmut_8, 
    'x__score_radon_cc__mutmut_9': x__score_radon_cc__mutmut_9, 
    'x__score_radon_cc__mutmut_10': x__score_radon_cc__mutmut_10, 
    'x__score_radon_cc__mutmut_11': x__score_radon_cc__mutmut_11, 
    'x__score_radon_cc__mutmut_12': x__score_radon_cc__mutmut_12, 
    'x__score_radon_cc__mutmut_13': x__score_radon_cc__mutmut_13, 
    'x__score_radon_cc__mutmut_14': x__score_radon_cc__mutmut_14, 
    'x__score_radon_cc__mutmut_15': x__score_radon_cc__mutmut_15, 
    'x__score_radon_cc__mutmut_16': x__score_radon_cc__mutmut_16, 
    'x__score_radon_cc__mutmut_17': x__score_radon_cc__mutmut_17, 
    'x__score_radon_cc__mutmut_18': x__score_radon_cc__mutmut_18, 
    'x__score_radon_cc__mutmut_19': x__score_radon_cc__mutmut_19, 
    'x__score_radon_cc__mutmut_20': x__score_radon_cc__mutmut_20, 
    'x__score_radon_cc__mutmut_21': x__score_radon_cc__mutmut_21, 
    'x__score_radon_cc__mutmut_22': x__score_radon_cc__mutmut_22, 
    'x__score_radon_cc__mutmut_23': x__score_radon_cc__mutmut_23, 
    'x__score_radon_cc__mutmut_24': x__score_radon_cc__mutmut_24
}

def _score_radon_cc(*args, **kwargs):
    result = _mutmut_trampoline(x__score_radon_cc__mutmut_orig, x__score_radon_cc__mutmut_mutants, args, kwargs)
    return result 

_score_radon_cc.__signature__ = _mutmut_signature(x__score_radon_cc__mutmut_orig)
x__score_radon_cc__mutmut_orig.__name__ = 'x__score_radon_cc'


def x__score_radon_mi__mutmut_orig(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_1(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = None
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_2(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(None)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_3(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = None
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_4(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["XXmiXX"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_5(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["MI"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_6(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) or isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_7(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(None, 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_8(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), None) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_9(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_10(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), ) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_11(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) * len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_12(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(None) / len(mis), 1) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score


def x__score_radon_mi__mutmut_13(output: str, _returncode: int) -> Optional[float]:
    """Score radon mi -j.  Average Maintainability Index across all files (0-100).

    Returns None on JSON parse failure so compute_tool_score propagates None
    rather than silently awarding 100 for a tool crash.
    """
    import json as _json
    try:
        data = _json.loads(output)
        # radon mi -j: {"file.py": {"mi": 80.5, "rank": "A"}}
        mis = [
            v["mi"]
            for v in data.values()
            if isinstance(v, dict) and isinstance(v.get("mi"), (int, float))
        ]
        # No analysable file → None (NOT a free 100). A passing readability score
        # with nothing to analyse is unverifiable; cross-validation blocks it.
        return round(sum(mis) / len(mis), 2) if mis else None
    except (_json.JSONDecodeError, ValueError):
        return None  # Tool crash / non-JSON stderr — cannot score

x__score_radon_mi__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_radon_mi__mutmut_1': x__score_radon_mi__mutmut_1, 
    'x__score_radon_mi__mutmut_2': x__score_radon_mi__mutmut_2, 
    'x__score_radon_mi__mutmut_3': x__score_radon_mi__mutmut_3, 
    'x__score_radon_mi__mutmut_4': x__score_radon_mi__mutmut_4, 
    'x__score_radon_mi__mutmut_5': x__score_radon_mi__mutmut_5, 
    'x__score_radon_mi__mutmut_6': x__score_radon_mi__mutmut_6, 
    'x__score_radon_mi__mutmut_7': x__score_radon_mi__mutmut_7, 
    'x__score_radon_mi__mutmut_8': x__score_radon_mi__mutmut_8, 
    'x__score_radon_mi__mutmut_9': x__score_radon_mi__mutmut_9, 
    'x__score_radon_mi__mutmut_10': x__score_radon_mi__mutmut_10, 
    'x__score_radon_mi__mutmut_11': x__score_radon_mi__mutmut_11, 
    'x__score_radon_mi__mutmut_12': x__score_radon_mi__mutmut_12, 
    'x__score_radon_mi__mutmut_13': x__score_radon_mi__mutmut_13
}

def _score_radon_mi(*args, **kwargs):
    result = _mutmut_trampoline(x__score_radon_mi__mutmut_orig, x__score_radon_mi__mutmut_mutants, args, kwargs)
    return result 

_score_radon_mi.__signature__ = _mutmut_signature(x__score_radon_mi__mutmut_orig)
x__score_radon_mi__mutmut_orig.__name__ = 'x__score_radon_mi'




def x__score_pytest_benchmark__mutmut_orig(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_1(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode != 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_2(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 6:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_3(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = None
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_4(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(None, output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_5(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", None, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_6(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, None)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_7(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_8(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_9(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, )
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_10(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"XXName\s+\(time\s+in\s+(ms|us|ns|s)\)XX", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_11(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_12(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"NAME\s+\(TIME\s+IN\s+(MS|US|NS|S)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_13(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = None
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_14(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).upper()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_15(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(None).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_16(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(2).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_17(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = None
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_18(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(None, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_19(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, None)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_20(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_21(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, )
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_22(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"XXmsXX": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_23(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"MS": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_24(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 2.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_25(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "XXusXX": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_26(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "US": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_27(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 1.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_28(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "XXnsXX": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_29(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "NS": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_30(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 1.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_31(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "XXsXX": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_32(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "S": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_33(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1001.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_34(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 2.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_35(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = None  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_36(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 2.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_37(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = None
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_38(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        None,
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_39(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        None,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_40(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_41(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_42(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"XX^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$XX",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_43(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_44(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(TEST_\S+)\s+([\d.]+(?:E[+-]?\d+)?)\s+([\d.]+(?:E[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_45(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = None
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_46(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 101.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_47(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(None):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_48(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = None
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_49(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) / to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_50(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(None) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_51(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(None)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_52(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(3)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_53(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms >= 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_54(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3001.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_55(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score = 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_56(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score += 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_57(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 51.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_58(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms >= 1000.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_59(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1001.0:
            score -= 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_60(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score = 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_61(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score += 25.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_62(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 26.0
    return max(0.0, score)




def x__score_pytest_benchmark__mutmut_63(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(None, score)




def x__score_pytest_benchmark__mutmut_64(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, None)




def x__score_pytest_benchmark__mutmut_65(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(score)




def x__score_pytest_benchmark__mutmut_66(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(0.0, )




def x__score_pytest_benchmark__mutmut_67(output: str, returncode: int) -> Optional[float]:
    """Score pytest-benchmark text output.

    Exit code 5 means no benchmark tests collected → return None (dimension skipped).
    Otherwise parse the benchmark table for mean latencies and penalise slow benchmarks.

    Thresholds (cross-validation heuristics, not NFR targets):
      mean > 3000 ms → -50 pts per benchmark  (hard fail, clearly exceeds NFR-01 target)
      mean > 1000 ms → -25 pts per benchmark  (warning zone)
      All within 1000 ms → 100
    """
    if returncode == 5:
        return None  # No benchmark tests exist yet — dimension not yet applicable

    # Parse the unit multiplier from the header line:  "Name (time in ms)"
    unit_m = re.search(r"Name\s+\(time\s+in\s+(ms|us|ns|s)\)", output, re.IGNORECASE)
    if unit_m:
        unit = unit_m.group(1).lower()
        to_ms = {"ms": 1.0, "us": 0.001, "ns": 0.000001, "s": 1000.0}.get(unit, 1.0)
    else:
        to_ms = 1.0  # Assume ms if header not found

    # Each data row: "  test_name   <mean_val>   <max_val>"
    # --benchmark-columns mean,max produces exactly those two numeric columns.
    row_re = re.compile(
        r"^\s*(test_\S+)\s+([\d.]+(?:e[+-]?\d+)?)\s+([\d.]+(?:e[+-]?\d+)?)\s*$",
        re.MULTILINE,
    )
    score = 100.0
    for m in row_re.finditer(output):
        mean_ms = float(m.group(2)) * to_ms
        if mean_ms > 3000.0:
            score -= 50.0
        elif mean_ms > 1000.0:
            score -= 25.0
    return max(1.0, score)

x__score_pytest_benchmark__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_pytest_benchmark__mutmut_1': x__score_pytest_benchmark__mutmut_1, 
    'x__score_pytest_benchmark__mutmut_2': x__score_pytest_benchmark__mutmut_2, 
    'x__score_pytest_benchmark__mutmut_3': x__score_pytest_benchmark__mutmut_3, 
    'x__score_pytest_benchmark__mutmut_4': x__score_pytest_benchmark__mutmut_4, 
    'x__score_pytest_benchmark__mutmut_5': x__score_pytest_benchmark__mutmut_5, 
    'x__score_pytest_benchmark__mutmut_6': x__score_pytest_benchmark__mutmut_6, 
    'x__score_pytest_benchmark__mutmut_7': x__score_pytest_benchmark__mutmut_7, 
    'x__score_pytest_benchmark__mutmut_8': x__score_pytest_benchmark__mutmut_8, 
    'x__score_pytest_benchmark__mutmut_9': x__score_pytest_benchmark__mutmut_9, 
    'x__score_pytest_benchmark__mutmut_10': x__score_pytest_benchmark__mutmut_10, 
    'x__score_pytest_benchmark__mutmut_11': x__score_pytest_benchmark__mutmut_11, 
    'x__score_pytest_benchmark__mutmut_12': x__score_pytest_benchmark__mutmut_12, 
    'x__score_pytest_benchmark__mutmut_13': x__score_pytest_benchmark__mutmut_13, 
    'x__score_pytest_benchmark__mutmut_14': x__score_pytest_benchmark__mutmut_14, 
    'x__score_pytest_benchmark__mutmut_15': x__score_pytest_benchmark__mutmut_15, 
    'x__score_pytest_benchmark__mutmut_16': x__score_pytest_benchmark__mutmut_16, 
    'x__score_pytest_benchmark__mutmut_17': x__score_pytest_benchmark__mutmut_17, 
    'x__score_pytest_benchmark__mutmut_18': x__score_pytest_benchmark__mutmut_18, 
    'x__score_pytest_benchmark__mutmut_19': x__score_pytest_benchmark__mutmut_19, 
    'x__score_pytest_benchmark__mutmut_20': x__score_pytest_benchmark__mutmut_20, 
    'x__score_pytest_benchmark__mutmut_21': x__score_pytest_benchmark__mutmut_21, 
    'x__score_pytest_benchmark__mutmut_22': x__score_pytest_benchmark__mutmut_22, 
    'x__score_pytest_benchmark__mutmut_23': x__score_pytest_benchmark__mutmut_23, 
    'x__score_pytest_benchmark__mutmut_24': x__score_pytest_benchmark__mutmut_24, 
    'x__score_pytest_benchmark__mutmut_25': x__score_pytest_benchmark__mutmut_25, 
    'x__score_pytest_benchmark__mutmut_26': x__score_pytest_benchmark__mutmut_26, 
    'x__score_pytest_benchmark__mutmut_27': x__score_pytest_benchmark__mutmut_27, 
    'x__score_pytest_benchmark__mutmut_28': x__score_pytest_benchmark__mutmut_28, 
    'x__score_pytest_benchmark__mutmut_29': x__score_pytest_benchmark__mutmut_29, 
    'x__score_pytest_benchmark__mutmut_30': x__score_pytest_benchmark__mutmut_30, 
    'x__score_pytest_benchmark__mutmut_31': x__score_pytest_benchmark__mutmut_31, 
    'x__score_pytest_benchmark__mutmut_32': x__score_pytest_benchmark__mutmut_32, 
    'x__score_pytest_benchmark__mutmut_33': x__score_pytest_benchmark__mutmut_33, 
    'x__score_pytest_benchmark__mutmut_34': x__score_pytest_benchmark__mutmut_34, 
    'x__score_pytest_benchmark__mutmut_35': x__score_pytest_benchmark__mutmut_35, 
    'x__score_pytest_benchmark__mutmut_36': x__score_pytest_benchmark__mutmut_36, 
    'x__score_pytest_benchmark__mutmut_37': x__score_pytest_benchmark__mutmut_37, 
    'x__score_pytest_benchmark__mutmut_38': x__score_pytest_benchmark__mutmut_38, 
    'x__score_pytest_benchmark__mutmut_39': x__score_pytest_benchmark__mutmut_39, 
    'x__score_pytest_benchmark__mutmut_40': x__score_pytest_benchmark__mutmut_40, 
    'x__score_pytest_benchmark__mutmut_41': x__score_pytest_benchmark__mutmut_41, 
    'x__score_pytest_benchmark__mutmut_42': x__score_pytest_benchmark__mutmut_42, 
    'x__score_pytest_benchmark__mutmut_43': x__score_pytest_benchmark__mutmut_43, 
    'x__score_pytest_benchmark__mutmut_44': x__score_pytest_benchmark__mutmut_44, 
    'x__score_pytest_benchmark__mutmut_45': x__score_pytest_benchmark__mutmut_45, 
    'x__score_pytest_benchmark__mutmut_46': x__score_pytest_benchmark__mutmut_46, 
    'x__score_pytest_benchmark__mutmut_47': x__score_pytest_benchmark__mutmut_47, 
    'x__score_pytest_benchmark__mutmut_48': x__score_pytest_benchmark__mutmut_48, 
    'x__score_pytest_benchmark__mutmut_49': x__score_pytest_benchmark__mutmut_49, 
    'x__score_pytest_benchmark__mutmut_50': x__score_pytest_benchmark__mutmut_50, 
    'x__score_pytest_benchmark__mutmut_51': x__score_pytest_benchmark__mutmut_51, 
    'x__score_pytest_benchmark__mutmut_52': x__score_pytest_benchmark__mutmut_52, 
    'x__score_pytest_benchmark__mutmut_53': x__score_pytest_benchmark__mutmut_53, 
    'x__score_pytest_benchmark__mutmut_54': x__score_pytest_benchmark__mutmut_54, 
    'x__score_pytest_benchmark__mutmut_55': x__score_pytest_benchmark__mutmut_55, 
    'x__score_pytest_benchmark__mutmut_56': x__score_pytest_benchmark__mutmut_56, 
    'x__score_pytest_benchmark__mutmut_57': x__score_pytest_benchmark__mutmut_57, 
    'x__score_pytest_benchmark__mutmut_58': x__score_pytest_benchmark__mutmut_58, 
    'x__score_pytest_benchmark__mutmut_59': x__score_pytest_benchmark__mutmut_59, 
    'x__score_pytest_benchmark__mutmut_60': x__score_pytest_benchmark__mutmut_60, 
    'x__score_pytest_benchmark__mutmut_61': x__score_pytest_benchmark__mutmut_61, 
    'x__score_pytest_benchmark__mutmut_62': x__score_pytest_benchmark__mutmut_62, 
    'x__score_pytest_benchmark__mutmut_63': x__score_pytest_benchmark__mutmut_63, 
    'x__score_pytest_benchmark__mutmut_64': x__score_pytest_benchmark__mutmut_64, 
    'x__score_pytest_benchmark__mutmut_65': x__score_pytest_benchmark__mutmut_65, 
    'x__score_pytest_benchmark__mutmut_66': x__score_pytest_benchmark__mutmut_66, 
    'x__score_pytest_benchmark__mutmut_67': x__score_pytest_benchmark__mutmut_67
}

def _score_pytest_benchmark(*args, **kwargs):
    result = _mutmut_trampoline(x__score_pytest_benchmark__mutmut_orig, x__score_pytest_benchmark__mutmut_mutants, args, kwargs)
    return result 

_score_pytest_benchmark.__signature__ = _mutmut_signature(x__score_pytest_benchmark__mutmut_orig)
x__score_pytest_benchmark__mutmut_orig.__name__ = 'x__score_pytest_benchmark'


def x__score_assertion_quality__mutmut_orig(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_1(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = None
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_2(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(None)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_3(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = None
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_4(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(None)
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_5(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get(None, 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_6(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", None))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_7(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get(0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_8(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", ))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_9(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("XXtotalXX", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_10(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("TOTAL", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_11(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 1))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_12(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = None
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_13(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(None)
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_14(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get(None, 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_15(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", None))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_16(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get(0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_17(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", ))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_18(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("XXassertedXX", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_19(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("ASSERTED", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_20(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 1))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_21(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total != 0:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_22(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 1:
        return 0.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_23(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 1.0
    return round(100.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_24(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(None, 1)


def x__score_assertion_quality__mutmut_25(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, None)


def x__score_assertion_quality__mutmut_26(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(1)


def x__score_assertion_quality__mutmut_27(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, )


def x__score_assertion_quality__mutmut_28(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted * total, 1)


def x__score_assertion_quality__mutmut_29(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 / asserted / total, 1)


def x__score_assertion_quality__mutmut_30(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(101.0 * asserted / total, 1)


def x__score_assertion_quality__mutmut_31(output: str, _returncode: int) -> Optional[float]:
    """Score ast-assertions output.  100 × (asserted / total).

    total == 0 (no test functions at all) → 0.0 — a project claiming a passing
    assertion-quality score with zero tests is a fabrication.  JSON parse failure
    → None (treat as tool error, do not silently award a score).
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        asserted = int(data.get("asserted", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 0.0
    return round(100.0 * asserted / total, 2)

x__score_assertion_quality__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_assertion_quality__mutmut_1': x__score_assertion_quality__mutmut_1, 
    'x__score_assertion_quality__mutmut_2': x__score_assertion_quality__mutmut_2, 
    'x__score_assertion_quality__mutmut_3': x__score_assertion_quality__mutmut_3, 
    'x__score_assertion_quality__mutmut_4': x__score_assertion_quality__mutmut_4, 
    'x__score_assertion_quality__mutmut_5': x__score_assertion_quality__mutmut_5, 
    'x__score_assertion_quality__mutmut_6': x__score_assertion_quality__mutmut_6, 
    'x__score_assertion_quality__mutmut_7': x__score_assertion_quality__mutmut_7, 
    'x__score_assertion_quality__mutmut_8': x__score_assertion_quality__mutmut_8, 
    'x__score_assertion_quality__mutmut_9': x__score_assertion_quality__mutmut_9, 
    'x__score_assertion_quality__mutmut_10': x__score_assertion_quality__mutmut_10, 
    'x__score_assertion_quality__mutmut_11': x__score_assertion_quality__mutmut_11, 
    'x__score_assertion_quality__mutmut_12': x__score_assertion_quality__mutmut_12, 
    'x__score_assertion_quality__mutmut_13': x__score_assertion_quality__mutmut_13, 
    'x__score_assertion_quality__mutmut_14': x__score_assertion_quality__mutmut_14, 
    'x__score_assertion_quality__mutmut_15': x__score_assertion_quality__mutmut_15, 
    'x__score_assertion_quality__mutmut_16': x__score_assertion_quality__mutmut_16, 
    'x__score_assertion_quality__mutmut_17': x__score_assertion_quality__mutmut_17, 
    'x__score_assertion_quality__mutmut_18': x__score_assertion_quality__mutmut_18, 
    'x__score_assertion_quality__mutmut_19': x__score_assertion_quality__mutmut_19, 
    'x__score_assertion_quality__mutmut_20': x__score_assertion_quality__mutmut_20, 
    'x__score_assertion_quality__mutmut_21': x__score_assertion_quality__mutmut_21, 
    'x__score_assertion_quality__mutmut_22': x__score_assertion_quality__mutmut_22, 
    'x__score_assertion_quality__mutmut_23': x__score_assertion_quality__mutmut_23, 
    'x__score_assertion_quality__mutmut_24': x__score_assertion_quality__mutmut_24, 
    'x__score_assertion_quality__mutmut_25': x__score_assertion_quality__mutmut_25, 
    'x__score_assertion_quality__mutmut_26': x__score_assertion_quality__mutmut_26, 
    'x__score_assertion_quality__mutmut_27': x__score_assertion_quality__mutmut_27, 
    'x__score_assertion_quality__mutmut_28': x__score_assertion_quality__mutmut_28, 
    'x__score_assertion_quality__mutmut_29': x__score_assertion_quality__mutmut_29, 
    'x__score_assertion_quality__mutmut_30': x__score_assertion_quality__mutmut_30, 
    'x__score_assertion_quality__mutmut_31': x__score_assertion_quality__mutmut_31
}

def _score_assertion_quality(*args, **kwargs):
    result = _mutmut_trampoline(x__score_assertion_quality__mutmut_orig, x__score_assertion_quality__mutmut_mutants, args, kwargs)
    return result 

_score_assertion_quality.__signature__ = _mutmut_signature(x__score_assertion_quality__mutmut_orig)
x__score_assertion_quality__mutmut_orig.__name__ = 'x__score_assertion_quality'


def x__score_error_handling_coverage__mutmut_orig(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_1(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = None
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_2(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(None)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_3(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = None
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_4(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(None)
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_5(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get(None, 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_6(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", None))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_7(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get(0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_8(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", ))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_9(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("XXtotalXX", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_10(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("TOTAL", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_11(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 1))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_12(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = None
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_13(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(None)
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_14(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get(None, 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_15(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", None))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_16(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get(0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_17(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", ))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_18(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("XXwith_handlerXX", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_19(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("WITH_HANDLER", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_20(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 1))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_21(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total != 0:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_22(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 1:
        return 100.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_23(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 101.0
    return round(100.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_24(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(None, 1)


def x__score_error_handling_coverage__mutmut_25(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, None)


def x__score_error_handling_coverage__mutmut_26(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(1)


def x__score_error_handling_coverage__mutmut_27(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, )


def x__score_error_handling_coverage__mutmut_28(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler * total, 1)


def x__score_error_handling_coverage__mutmut_29(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 / with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_30(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(101.0 * with_handler / total, 1)


def x__score_error_handling_coverage__mutmut_31(output: str, _returncode: int) -> Optional[float]:
    """Score ast-error-handling output.  100 × (files_with_handler / total_files).

    total == 0 (no source files with code) → 100.0 — nothing to handle is not a
    failure.  JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_handler = int(data.get("with_handler", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_handler / total, 2)

x__score_error_handling_coverage__mutmut_mutants : ClassVar[MutantDict] = {
'x__score_error_handling_coverage__mutmut_1': x__score_error_handling_coverage__mutmut_1, 
    'x__score_error_handling_coverage__mutmut_2': x__score_error_handling_coverage__mutmut_2, 
    'x__score_error_handling_coverage__mutmut_3': x__score_error_handling_coverage__mutmut_3, 
    'x__score_error_handling_coverage__mutmut_4': x__score_error_handling_coverage__mutmut_4, 
    'x__score_error_handling_coverage__mutmut_5': x__score_error_handling_coverage__mutmut_5, 
    'x__score_error_handling_coverage__mutmut_6': x__score_error_handling_coverage__mutmut_6, 
    'x__score_error_handling_coverage__mutmut_7': x__score_error_handling_coverage__mutmut_7, 
    'x__score_error_handling_coverage__mutmut_8': x__score_error_handling_coverage__mutmut_8, 
    'x__score_error_handling_coverage__mutmut_9': x__score_error_handling_coverage__mutmut_9, 
    'x__score_error_handling_coverage__mutmut_10': x__score_error_handling_coverage__mutmut_10, 
    'x__score_error_handling_coverage__mutmut_11': x__score_error_handling_coverage__mutmut_11, 
    'x__score_error_handling_coverage__mutmut_12': x__score_error_handling_coverage__mutmut_12, 
    'x__score_error_handling_coverage__mutmut_13': x__score_error_handling_coverage__mutmut_13, 
    'x__score_error_handling_coverage__mutmut_14': x__score_error_handling_coverage__mutmut_14, 
    'x__score_error_handling_coverage__mutmut_15': x__score_error_handling_coverage__mutmut_15, 
    'x__score_error_handling_coverage__mutmut_16': x__score_error_handling_coverage__mutmut_16, 
    'x__score_error_handling_coverage__mutmut_17': x__score_error_handling_coverage__mutmut_17, 
    'x__score_error_handling_coverage__mutmut_18': x__score_error_handling_coverage__mutmut_18, 
    'x__score_error_handling_coverage__mutmut_19': x__score_error_handling_coverage__mutmut_19, 
    'x__score_error_handling_coverage__mutmut_20': x__score_error_handling_coverage__mutmut_20, 
    'x__score_error_handling_coverage__mutmut_21': x__score_error_handling_coverage__mutmut_21, 
    'x__score_error_handling_coverage__mutmut_22': x__score_error_handling_coverage__mutmut_22, 
    'x__score_error_handling_coverage__mutmut_23': x__score_error_handling_coverage__mutmut_23, 
    'x__score_error_handling_coverage__mutmut_24': x__score_error_handling_coverage__mutmut_24, 
    'x__score_error_handling_coverage__mutmut_25': x__score_error_handling_coverage__mutmut_25, 
    'x__score_error_handling_coverage__mutmut_26': x__score_error_handling_coverage__mutmut_26, 
    'x__score_error_handling_coverage__mutmut_27': x__score_error_handling_coverage__mutmut_27, 
    'x__score_error_handling_coverage__mutmut_28': x__score_error_handling_coverage__mutmut_28, 
    'x__score_error_handling_coverage__mutmut_29': x__score_error_handling_coverage__mutmut_29, 
    'x__score_error_handling_coverage__mutmut_30': x__score_error_handling_coverage__mutmut_30, 
    'x__score_error_handling_coverage__mutmut_31': x__score_error_handling_coverage__mutmut_31
}

def _score_error_handling_coverage(*args, **kwargs):
    result = _mutmut_trampoline(x__score_error_handling_coverage__mutmut_orig, x__score_error_handling_coverage__mutmut_mutants, args, kwargs)
    return result 

_score_error_handling_coverage.__signature__ = _mutmut_signature(x__score_error_handling_coverage__mutmut_orig)
x__score_error_handling_coverage__mutmut_orig.__name__ = 'x__score_error_handling_coverage'


def x__score_docstring_coverage__mutmut_orig(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_1(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = None
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_2(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(None)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_3(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = None
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_4(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(None)
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_5(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get(None, 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_6(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", None))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_7(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get(0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_8(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", ))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_9(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("XXtotalXX", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_10(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("TOTAL", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_11(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 1))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_12(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = None
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_13(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(None)
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_14(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get(None, 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_15(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", None))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_16(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get(0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_17(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", ))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_18(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("XXwith_docXX", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_19(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("WITH_DOC", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_20(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 1))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_21(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total != 0:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_22(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 1:
        return 100.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_23(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 101.0
    return round(100.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_24(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(None, 1)


def x__score_docstring_coverage__mutmut_25(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, None)


def x__score_docstring_coverage__mutmut_26(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(1)


def x__score_docstring_coverage__mutmut_27(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc / total, )


def x__score_docstring_coverage__mutmut_28(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 * with_doc * total, 1)


def x__score_docstring_coverage__mutmut_29(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(100.0 / with_doc / total, 1)


def x__score_docstring_coverage__mutmut_30(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
    return round(101.0 * with_doc / total, 1)


def x__score_docstring_coverage__mutmut_31(output: str, _returncode: int) -> Optional[float]:
    """Score ast-docstrings output.  100 × (public_with_docstring / total_public).

    total == 0 (no public API) → 100.0 — nothing to document is not a failure.
    JSON parse failure → None.
    """
    import json as _json
    try:
        data = _json.loads(output)
        total = int(data.get("total", 0))
        with_doc = int(data.get("with_doc", 0))
    except (_json.JSONDecodeError, ValueError, TypeError):
        return None
    if total == 0:
        return 100.0
