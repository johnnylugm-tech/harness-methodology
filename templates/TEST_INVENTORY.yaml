# TEST_INVENTORY.yaml — P1 交付物：必需測試函式清單
format_version: "1.0"

fr_tests:
  FR-01:
    unit:
      - test_fr01_example_unit
    integration:
      - test_fr01_example_integration

cross_cutting:
  security:
    - test_security_example
  deployment:
    - test_deployment_example
