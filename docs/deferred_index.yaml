# GENERATED by scripts/extract_deferred_index.py — do not hand-edit.
#
# Every `item`/`reason`/`reopen`/`text` value below is a byte-exact slice of
# docs/PROPOSAL_ADJUDICATIONS.md. tests/test_deferred_index.py asserts it, so
# this file cannot say anything the ledger does not say. Round 80 declined to
# build it because a REWRITING extraction risks putting words in past rounds'
# mouths; this one copies, and the guard is what makes that a property rather
# than an intention.
#
# `kind: unstructured` means the extractor did not recognise the section's
# shape and declined to guess. That is a hole this file admits to, not one it
# hides — see Round 46.
#
# The guard mapping lives in docs/deferred_guards.yaml, because which test
# measures a re-open condition is a judgement and this file is a copy.
entries:
  - round: 24
    line: 280
    kind: table_row
    item: "resume 粒度 phase→task"
    reason: "需 workflow runtime 支援 task 級 checkpoint。老闆已用「重跑一個 phase」解決(代價:Gate4 重跑一次同分 97.4)"
    reopen: "runtime 提供 checkpoint API"
  - round: 24
    line: 281
    kind: table_row
    item: "workflow dispatch 進 run-report 的帳"
    reason: "workflow JS 是 hermetic(無 fs),`agent()` 不經 harness spawner。**沒有正解**,只有讀 runtime 內部 journal 這種脆弱做法"
    reopen: "runtime 提供 dispatch 匯出"
  - round: 24
    line: 282
    kind: table_row
    item: "P3 dispatch 發散"
    reason: "上一輪用「P3 29 次 vs P5 5 次 = 6 倍」論證,但 P3 是唯一跑完整 RED/GREEN/IMPROVE 的 phase,比較不公平,**論據已自行收回**。真正異常的是 3 TIMEOUT + 2 ERROR 全部集中在 P3 —— n=1"
    reopen: "下次 E2E 重現同樣分佈"
  - round: 24
    line: 283
    kind: table_row
    item: "Agent B reason 的語意驗證"
    reason: "見 R24-2c 邊界"
    reopen: "出現不需要 LLM 判定的機械做法"
  - round: 25
    line: 401
    kind: bullet
    text: |-
      - 其餘約 12 個 pytest 站點（`stage_pass_generator`、`cross_artifact`、
        `confidence_scorer`、`auto_fix/strategies`、`harness/toolchains/registry`）不動 ——
        它們不在 advance-phase 的同一次呼叫裡，一起改是失控重構。
  - round: 25
    line: 404
    kind: bullet
    text: |-
      - gitleaks / ruff / mypy 不刪。它們與 gate 維度重疊但**尺不同**（gate linting 門檻 90
        ≙ 容 5 個違規，advance 要 0；gate 用 pyright，advance 用 mypy）。刪掉是降標。合計 0.35s。
  - round: 26
    line: 488
    kind: bullet
    text: |-
      - **per-gate schema 拆分**：修正 1 之後前提消失（不是「四個 gate 形狀不同」，是
        「required 列了沒人被要求寫的欄位」）。
  - round: 26
    line: 490
    kind: bullet
    text: |-
      - **render-from-SSOT 生成 gate-1 prompt 的 JSON 區塊**：考慮過。prompt 的逐欄指引
        散文（`// REQUIRED: count from pytest summary line`）在 schema 裡沒有家，而 R17 站1
        對同一類問題選的正是 registry + 完備性 meta-test 路線。採 parity 測試。
  - round: 26
    line: 493
    kind: bullet
    text: |-
      - **`tests_*` 三欄位的減法、`gate`→`gate_num` 統一**：見 R26-DEFER-1/2。動的是全系統
        派發最頻繁的 prompt，為純命名一致性不值得，且會churn golden。
  - round: 35
    line: 1246
    kind: unstructured
    heading: "### 一項計畫偏離（不做，記在此）"
    spans_lines: [1246, 1253]
  - round: 35
    line: 1270
    kind: bullet
    text: |-
      - **R33 站3 的 `type:` 缺席**：taskq-renew 的 12 個 NFR，SRS 機讀 block 的 `type:` 全部是 None，
        `illegal_nfr_vocabulary` 的 `if raw_type is not None` 跳過必填欄位（`templates/SRS.md:107`
        寫著必填），值到 SAD 的 SAB block 才首次出現——**上游缺席、下游發明**，是 R34 記的 F3
        形狀降一階。老闆本輪界定的是那三個缺陷；這是 SRS↔SAD 傳輸面的另一條線，另案。
  - round: 35
    line: 1274
    kind: bullet
    text: |-
      - **taskq-renew 專案本身不動**（全程唯讀，`git status --short` 指紋前後相同）。
        它的 Gate 2 會因本輪修復而能真正跑出 mutation 分數，何時重跑由老闆決定。
  - round: 36
    line: 1367
    kind: bullet
    text: |-
      - **不新增 pre-commit hook step**：現行 hook 全部 non-blocking，加一個會擋的
        step 是政策變更。測試已在 CI 擋住。若哪天 hook 政策改為可阻擋，一併重議。
  - round: 36
    line: 1369
    kind: bullet
    text: |-
      - **不新增 CI step**：測試套件本來就在 CI 跑，再加一個 step 就是第二份陳述，
        正是本輪在修的東西。
  - round: 36
    line: 1371
    kind: bullet
    text: |-
      - **`.methodology/phaseN_plan.md` 不納入 shipped-parity**：那是 per-project
        執行期狀態，`plan-all` 每次重生，與受版控的 build artifact 不同類。
        若它哪天變成受版控的交付面再開。
  - round: 36
    line: 1374
    kind: bullet
    text: |-
      - **`47ec3fd` 那次翻轉本身不重議**：本輪認定 `_DEFAULTS` 為權威、其餘五份為錯。
        若老闆的意思是預設該關，那是**另一個決定**（改 `_DEFAULTS` 一處），
        本輪的機制修復不受影響——這正是把值收斂到一處的好處。
  - round: 37
    line: 1470
    kind: bullet
    text: |-
      - **doctor 不接 CI verdict**（雖然核准的計畫寫了）：每次 doctor 都發一次
        `gh run list` 會讓這個離線的、at-rest 的跨檔一致性檢查變成網路相依。
        push path 才是執法點，degradation ledger 已記錄。理由留在 `core/doctor.py`
        原本要放檢查的位置。若 doctor 哪天長出 `--online` 模式再開。
  - round: 37
    line: 1474
    kind: bullet
    text: |-
      - **無 origin remote ⇒ post-push CI gate 回 0**（並印出「not applicable」）：
        沒有 remote 就沒有 CI 可以紅。這是唯一一種「真的不適用」而非「不知道」的
        unavailable，明講而不是靜默跳過。
  - round: 37
    line: 1477
    kind: bullet
    text: |-
      - **`ci_verdict_wait` 不進 harness_config**：一個常數 + `--wait` 覆寫已足夠
        （taskq-renew 的 run 15s–2m）。若某專案真的需要 per-project 值再開。
  - round: 37
    line: 1479
    kind: bullet
    text: "- **非 git repo 路徑保持原行為**（退回 SKIP_DIRS），不動與本輪缺陷無關的專案。"
  - round: 37
    line: 1480
    kind: bullet
    text: |-
      - **taskq-renew 的 Gate 4 = 93.6 不重判**：那是舊判定的效力問題，屬裁決不屬修缺陷。
        本輪只保證下一次量的是對的樹。
  - round: 38
    line: 1576
    kind: bullet
    text: |-
      - **R38-DEFER-1**：`harness_config` 的 `is_dim_disabled("architecture")` 能讓
        `crg-arch-check` 直接 `return 0`，且沒有審計軌跡。這是第四個執法者。本輪不動
        （會牽動所有維度的 disable 語意）。老闆要收緊再開。
  - round: 38
    line: 1579
    kind: bullet
    text: |-
      - **R38-DEFER-2**：prompt 散文仍逐一列舉每個維度的門檻（`15 dims: linting(90)
        … architecture(80) …`），`pass_line_desc` 也重述「CRG architecture ≥80」。
        同一缺陷跨 13 維度 3 gate，是比本輪更大的一次改動。
  - round: 38
    line: 1582
    kind: bullet
    text: "- **不重判 taskq-renew 的 Gate 4 = 93.6**：舊判定的效力屬裁決不屬修缺陷。"
  - round: 38
    line: 1583
    kind: bullet
    text: |-
      - **不移除 `devil_advocate_evidence` 驗證**：Gate 4 A3 對所有 Tier-3 維度仍用它，
        與豁免無關。
  - round: 38
    line: 1585
    kind: bullet
    text: |-
      - **不對既有專案 grandfather `gate_verify` 記錄**：第一次會被擋，跑一次
        `verify-gate` 即可。允許無記錄通過等於本輪什麼都沒做。
  - round: 39
    line: 1709
    kind: bullet
    text: "- **不移除 disable 機制**：老闆已裁定保留。"
  - round: 39
    line: 1710
    kind: bullet
    text: |-
      - **不改任何門檻數值**：本輪只改「誰說、說得對不對」，不改「是多少」。gate 3 /
        gate 4 的計畫散文 render 後與手寫字串**位元組相同**。
  - round: 39
    line: 1712
    kind: bullet
    text: "- **不重判既有專案的歷史 gate 結果**。"
  - round: 39
    line: 1713
    kind: bullet
    text: |-
      - **既有專案的殘留欄位不做遷移**。唯讀量過四個消費專案，實測結果：

        | 專案 | 殘留 | 現在的行為 |
        |---|---|---|
        | `taskq` | manifest `da_waiver_applied: ["architecture"]` + `da_waiver_needs_human_review: true`；`gate4_result.json` `da_waiver` 與 `breakdown/architecture/da_waiver` | manifest 兩個欄位**已無任何消費者**（站1 移除），不影響判定 |
        | `taskq-renew` | 同上（無 breakdown 那一份） | 同上 |
        | `run-all-by-workflow` | `gate2/gate3_result.json` 的 `da_waiver: {}`；`gate4_result.json` 的 `{"architecture": true}` | 空 dict 不觸發拒絕（迴圈不進 body）；gate 4 那一份會 |
        | `taskq-plus` | 無 | — |

        非空的 `da_waiver` 在下一次該 gate 的 finalize-gate 會被 R38 站3 拒絕，訊息
        末行已指名動作（`Then remove da_waiver.<dim> from gateN_result.json and
        re-run`）。四個專案目前都在 phase 9，不會自然重跑 Gate 4；除非 CR 重開，
        否則不需要老闆做任何事。這是 R38 的既定行為，不是本輪新增。
  - round: 39
    line: 1727
    kind: bullet
    text: |-
      - **`taskq` / `taskq-plus` 的 `mutation_testing` 是關的**（實測）。它們的 Gate 4
        判定是在 14 個維度上做的，不是 15——這正是站2 從今以後會落盤的那種事實，
        但**不追溯既有判定**。
  - round: 40
    line: 1845
    kind: bullet
    text: |-
      - **不修 taskq-renew 的架構**。它是唯讀專案，且 57.1 的正解是拆 `storage-parser`，
        屬於該專案的工作。
  - round: 40
    line: 1847
    kind: bullet
    text: |-
      - **不降 `crg_cohesion_healthy` 讓 CI 轉綠**。把 0.25 降到 0.21 會讓兩個
        marginal 社群變健康、分數變 6/7 = 85.7、CI 立刻綠，而 97 人社群原封不動。
        這是 workaround，已向老闆說明存在但不建議。
  - round: 40
    line: 1850
    kind: bullet
    text: "- **不回填消費專案的 CI 檔**。doctor 現在會報，修復指令一行，何時執行是老闆的決定。"
  - round: 40
    line: 1851
    kind: bullet
    text: |-
      - **不收斂那 6 個倖存 CRG 預設值在兩份文件裡的重複**。那是本輪之前就有的重述債，
        不是本輪造成的，且收斂它要改一份 agent 會讀的 prompt。**再開條件**：下一次
        有人改動這 6 個數字中的任何一個時一併處理。
  - round: 41
    line: 1937
    kind: bullet
    text: |-
      - **TDD-IMPROVE 在綠 baseline 上「確實沒東西可重構」仍是錯誤。** 修完站2 後，
        它是唯一剩下的無 commit 合法結局而沒有表示法。屬**潛在**而非活傷口：taskq /
        taskq-plus / taskq-renew / taskq-api 每個 FR 都有 `refactor(FR-NN): IMPROVE`
        commit，沒有 agent 報過「沒東西可做」。正解需要決定「一個不產生 commit 的步驟
        如何記錄自己完成」，牽動 idempotency 契約，本輪刻意不碰。
        **再開條件**：任一專案的 IMPROVE 步驟回報無可重構。
  - round: 41
    line: 1943
    kind: bullet
    text: |-
      - **不碰 taskq-api**（量測期間 17:18–17:37 另有 session 在手動修它，`app.py` /
        `test_fr03.py` / `test_fr04.py` 陸續變動、`tests/conftest.py` 新建，而
        `sessions_spawn.log` 無新紀錄 → 不是 harness 發的；17:37 該 session 已讓
        FR-04 的 Gate 1 finalize）。全部驗收改用 `/tmp` clone pin 在 `0311a42`
        並還原當時的 sentinel，與他們的編輯無關。
  - round: 41
    line: 1948
    kind: bullet
    text: "- **不改任何門檻數值、不重判任何既有 gate 結果、不動 branch protection。**"
  - round: 41
    line: 1949
    kind: bullet
    text: "- **不為 INFRA_ERROR 加自動退避**：站3 只把分類建在正確的欄位上。"
  - round: 42
    line: 2042
    kind: bullet
    text: |-
      - **站4a 撤回，紅測試刪除而非改綠。** 計畫主張七型每個 FR 都要，而
        `derive_test_cases.md` 兩處說相反：「Classification drives which questions
        generate mandatory vs optional」、「Skip a pattern if it clearly does not
        apply」，Q7 標為 conditional。平版七選七會罰一個正確跳過的專案——**本輪自己
        的病灶**。查過最近的既有機制：trace 維度 4c 的 `nfr_untested` 對**兩個專案
        都是 `[]`**（plus 的測試逐一提到每個 NFR，即使 TEST_SPEC 零推導），所以它答
        的是別的問題。可執法的子規則是 prompt 唯一無條件的那句「Step-1b-forced
        patterns may NOT be skipped」，但那需要把 SAD 的 architecture-risk trait 解析
        成 forced 集合——本輪沒有量過那個輸入。**再開條件**：量到 forced 集合的解析
        路徑，或任一專案在 nfr_pattern 全缺的情況下仍過 Gate 4。
  - round: 42
    line: 2052
    kind: bullet
    text: |-
      - **不改 spec-coverage 的五個門檻**（前提3：宣告值）。再開條件：缺口清單上線後
        仍有專案在門檻內漏掉整類 NFR 測試。
  - round: 42
    line: 2054
    kind: bullet
    text: "- **不改 STRIDE 計分**（框架刻意只要求 per-boundary 覆蓋）。"
  - round: 42
    line: 2055
    kind: bullet
    text: "- **不給 `crg_cohesion_healthy` 加下限**（憑空定 floor 是發明門檻；正解是可見）。"
  - round: 42
    line: 2056
    kind: bullet
    text: |-
      - **不改 D6 的分數**：一個 12.5 KB 的檔內部形成兩個互不相連的叢集是不是架構
        問題，本輪沒有立場裁決，憑空排除等於重建 R38 移除的豁免。
  - round: 42
    line: 2058
    kind: bullet
    text: "- **不重判 taskq-plus / taskq-renew 的既有 gate 結果。**"
  - round: 43
    line: 2133
    kind: table_row
    item: "擴充 auto-fix strategy 覆蓋率"
    reason: "`SAD.md:164` 記錄移除理由是它們產出假修復（空 stub / 加註解）"
    reopen: "某個 strategy 能在真實專案上通過它宣稱要通過的 check，並留下可驗證收據"
  - round: 43
    line: 2134
    kind: table_row
    item: "移除 `scripts/hooks/pre-push:47-52` 用 HEAD subject 猜 phase 的啟發式"
    reason: "站2 消除了它的存在理由（handover commit 不再可能帶未清 obligation），但退場是後果不是本輪動作"
    reopen: "站2 上線後在真實專案量到 handover commit 不可能再帶未清 obligation"
  - round: 43
    line: 2135
    kind: table_row
    item: "preview 仍會 append degradation ledger"
    reason: "那個 degradation 是真的發生了（drift detector 退回空 baseline），R13 的規則是不得靜默遺忘。與「檢查修復它所量測的東西」不同類"
    reopen: "若量到 preview 的 ledger 記錄使 `run-report` 的降級統計失真"
  - round: 43
    line: 2136
    kind: table_row
    item: "改 spec-coverage / gate 門檻數字、改 STRIDE 計分、加任何 waiver"
    reason: "與 R42 同"
    reopen: "同 R42"
  - round: 45
    line: 2401
    kind: table_row
    item: "不動 `cli/gate_cmds.py` 的 `--fr-id` 不一致改寫語意"
    reason: "`b288c9d` 8/11 剛落地。本輪只讓「證據不見了」被說出來。改寫語意是否應改成整筆拒絕，是下一輪候選"
  - round: 45
    line: 2402
    kind: table_row
    item: "收據本身住在 gitignored 的 `.sessi-work/sentinels/`"
    reason: "**站3 在 fresh clone 上因此沉默。** 與站1 同一個病（證據住在不持久的地方），本輪不擴大到「把收據也搬進 `.methodology/`」——下一輪候選"
  - round: 45
    line: 2403
    kind: table_row
    item: "`cli/fr_prompts/gate.py` 有一段重複死碼"
    reason: "`spec_section` 在 21-31 行算一次、41-49 行覆寫一次。與本輪需求無關，**告知不刪**"
  - round: 45
    line: 2404
    kind: table_row
    item: "不改任何 gate 門檻、權重、維度集合的數值"
    reason: "站4 只改「誰來說」"
  - round: 48
    line: 2658
    kind: bullet
    text: |-
      - **不刪 auto_fix 的十二支函式與 30 條 CLASSIFICATION_TABLE 條目。**
        `tests/test_no_hardcoded_paths.py` 的 R20 站2 守衛以 `fix_low_coverage` 為**主體**，
        且該守衛登記在 `tests/REGRESSION_GUARDS.yaml`——刪碼會讓一條已登記守衛描述一個
        不存在的函式。守衛條目、測試、程式碼要在同一個 commit 退役，那是一輪減法自己的事。
        本輪改為**在 dispatch 處拒絕**，讓退役有執行者而非只有標籤。
  - round: 48
    line: 2663
    kind: bullet
    text: |-
      - **`EscalationCondition.LOW_CONFIDENCE` 本輪之後不再可經 `fix()` 到達。**
        唯一存活的 strategy 固定回報 90，所有回報低於 70 的都已退役。其單元測試改為直接
        驅動 `check_escalation`（對「階梯」的測試本來就該是這個形狀）。
        留一支會造假的 strategy 只為讓一個 escalation 保持可達，是本末倒置。
  - round: 48
    line: 2667
    kind: bullet
    text: "- **不做巢狀 workflow**（R23-A 已裁決，本輪不推翻）。"
  - round: 48
    line: 2668
    kind: bullet
    text: |-
      - **UNKNOWN 不觸發 harness 修復**（老闆裁決）。「證明不了是專案的錯就去改框架」
        等於給修復 agent 一個修改判定者的常設動機。
  - round: 48
    line: 2670
    kind: bullet
    text: |-
      - **CLAUDE.md 不是交接文件的家**：`**/CLAUDE.md` 全域 gitignore，只寫在那裡的
        交接永遠到不了消費專案。內容寫進 `docs/ERROR_HANDLING.md`。
  - round: 48
    line: 2672
    kind: bullet
    text: |-
      - **`core/doctor.py` 進入 god-file 清單**（882 → 923）。本輪開始前就在 882，
        任何新檢查都會越過 900 預設。散文縮過一次就停手——為閃過門檻而削註解不是門檻的用途。
        條目寫明：**下一個加進來的檢查應該拆檔，而不是再調高數字。**
  - round: 50
    line: 2779
    kind: bullet
    text: "- **不重判任何既有 gate 結果**（老闆裁決 + R38）。taskq-api 全程唯讀。"
  - round: 50
    line: 2780
    kind: bullet
    text: |-
      - **D2 本輪不修**。它的成因分兩半：計分面（agent 值算進分母）已由 D5 修掉；
        控制流面（`unverifiable` 為何沒讓 `finalize_gate` 抛 `GateBlockedError`）需要
        P1 的答案，而 P1 的證據已被刪除。**再開條件**：站6 落地後，下一次真實 run 若再
        出現「S4 記了 unverifiable 而 gate 仍 PASS」，`gate_evidence/harness_verification/`
        會留著那次的原始輸出，屆時可以直接定案。
  - round: 50
    line: 2785
    kind: bullet
    text: |-
      - **D6 不讓 workflow 側捕獲 cost**。計畫寫「cost 捕獲隨之涵蓋 workflow 側」，
        這條**前提是假的**：Workflow sandbox 沒有 process envelope 也沒有時鐘
        （R26 站5 自己記過）。正解是修**讀的那一端**——分母只除能回答那個問題的母體。
  - round: 50
    line: 2788
    kind: bullet
    text: |-
      - **不做 degradation ledger 與 workflow_blocks 的合併**。站3 先讓兩邊都帶座標，
        合併與否留待有人真的要在一條時間軸上讀它們時再議。
  - round: 50
    line: 2790
    kind: bullet
    text: |-
      - **D10 只解決「活過階段轉換」**。同一個 gate 重跑仍會覆寫前一次的 audit 檔。
        **再開條件**：當某輪需要比較同一個 gate 兩次執行的差異時，才做 per-run 保存。
  - round: 50
    line: 2792
    kind: bullet
    text: |-
      - **不加 doctor WARN 說「已定案判定裡有未交叉驗證維度」**（計畫站2 原本要做）。
        它唯一的生產者會抛 `GateBlockedError`，加了就是 R30 花一輪拆掉的半座機制。
  - round: 51
    line: 2891
    kind: bullet
    text: |-
      - **不重判任何既有 gate 結果**（老闆裁決 + R38）。六個 taskq 專案全程唯讀，
        本輪一個位元組都沒動它們。
  - round: 51
    line: 2893
    kind: bullet
    text: "- **不修 taskq-api 的空殼實作**。老闆裁決：只出診斷報告。"
  - round: 51
    line: 2894
    kind: bullet
    text: |-
      - **站5 攔不到 FR-09 真正斷掉的地方**——「SPEC 表格列沒有生出 AC」。把它機械化需要
        SPEC 結構化，或一個「AC 數 vs 規範性行數」的比例啟發式；**後者就是本輪母體本身**
        （再造一個代理指標）。**再開條件**：下一次真實 run 再出現同型（SPEC 有、AC 無），
        就是把 SPEC 結構化的觸發點。
  - round: 51
    line: 2898
    kind: bullet
    text: |-
      - **不對 FIX 家族其餘四支注入 SRS**。它們處理工具失敗，與需求無關；注入只會擴大
        prompt 而不改變任何判定。由
        `test_the_four_tool_fix_builders_stay_out_of_scope` 釘住。
  - round: 51
    line: 2901
    kind: bullet
    text: |-
      - **站3 不做硬 BLOCK**。patch 邊界是單元測試的正常做法；要的不是禁止，是讓
        「這個分數來自一套把被驗邊界換掉的測試」被寫下來。`score_source` 把它排出
        `weight_covered`，分數本身不動（R32 站4）。
  - round: 51
    line: 2904
    kind: bullet
    text: |-
      - **站3 不標 `mutation_testing`**。被 patch 掉的模組裡的 mutant 會存活，所以替身
        已經讓 mutation 分數**下降**，標記它會描述反方向。
  - round: 51
    line: 2906
    kind: bullet
    text: |-
      - **站4 不用 AST 重數被 omit 的 statements**。那會產出一個「看起來能跟 coverage.py
        比較、實際上不能」的數字——本輪母體的翻版。`coverage.json` 沒有的就報「size unknown」。
  - round: 51
    line: 2908
    kind: bullet
    text: |-
      - **「R45–R48 造成退化」這個因果沒有證明**。已證明的是：兩輪之間框架新增的每一項
        檢查都在測試面，產品面的檢查數量維持為零。這是**激勵解釋，不是因果證明**；
        反面情境（只是 agent 抽樣變異）無法排除。六站的修法各自單獨成立，與成因無關。
  - round: 52
    line: 3041
    kind: bullet
    text: |-
      - **不做跨專案離群警訊**。harness 是各專案的 submodule，看不到彼此的 run；做一份需
        人工維護的 checked-in 參考語料，就是又一個「宣告了沒有執行者」（R43 母體）。
        **再開條件：出現共用的 run 儲存處。**
  - round: 52
    line: 3044
    kind: bullet
    text: |-
      - **不讓框架自己知道怎麼啟動一個 FastAPI app**。那會綁死技術棧，與 language-agnostic
        直接衝突。**代價誠實寫下：啟動方式仍有一部分定義權在被判定方手上**，本輪只是把
        那份定義權的濫用變得可檢測，沒有收回它。
  - round: 52
    line: 3047
    kind: bullet
    text: "- **不修改任何 taskq-* 專案，不重判既有 gate 結果，不動門檻/權重/維度定義，不加 waiver。**"
  - round: 52
    line: 3048
    kind: bullet
    text: |-
      - **不把「缺少 verify-system target」做成第二個 block**：`make verify-system` 會非零
        退出，`execute_verification_target` 對 100 的門檻已經擋了；一個事實兩個執法者是
        R38 的缺陷。
  - round: 53
    line: 3166
    kind: bullet
    text: |-
      - **arch-constraints 維持只記錄**（老闆本輪裁決）。162 筆 ledger 保留。
        再開條件：P2 產出契約改成「宣告約束時必須指名執法者」。
  - round: 53
    line: 3168
    kind: bullet
    text: |-
      - **不修改任何 taskq-* 專案**（全程唯讀）。taskq-super 的 24 處 neutralised 與
        NFR-03/04 的 VERIFIED 宣稱是專案自己的債；本輪修完後它下一次 gate 會被逐條指名。
  - round: 53
    line: 3170
    kind: bullet
    text: "- **不改寫 `git add -A` 的整體行為**。"
  - round: 53
    line: 3171
    kind: bullet
    text: "- **不做 D4/D5/D7 的 JS 對等實作**（皆為 Python 專屬機制）；D3 做了 JS 對等。"
  - round: 53
    line: 3172
    kind: bullet
    text: |-
      - **一個順帶觀察，本輪不修**：taskq-super 的 Gate 4 breakdown 記
        `test_assertion_quality: 100.0`，而同一列的 `tool_evidence` 是
        `{"total": 318, "asserted": 292, ...}` — 即 91.8。S4 只在「框架說不及格而 agent
        說及格」時擋，門檻是 70，所以兩邊都過關、agent 的數字留下。這是 R50 站2
        （score_source）的地界，不是本輪的。記在這裡以免第三次被重新發現。
  - round: 53
    line: 3177
    kind: bullet
    text: "- **不 push。**"
  - round: 54
    line: 3297
    kind: bullet
    text: |-
      - **`declared_only` 不擋。** 再開條件：P2 產出契約改成「宣告約束時必須指名
        執法者」，那時第三態才會消失。
  - round: 54
    line: 3299
    kind: bullet
    text: "- **不把關鍵字放寬到裸 `layer`**（見 P6）。再開條件：同上。"
  - round: 54
    line: 3300
    kind: bullet
    text: |-
      - **不替 JS 專案做 bandit 對等**（semgrep-js 規則 ID 是另一套詞彙，此機器上零個
        JS 專案）。再開條件：出現宣告 shell/eval 類約束的 JS 專案。
  - round: 54
    line: 3302
    kind: bullet
    text: "- **不動 `phase_completed` 寫入順序**、**不做跨專案語料**（理由同上表 D/E）。"
  - round: 54
    line: 3303
    kind: bullet
    text: "- **不修改任何 taskq-* 專案，不重判既有 gate 結果，不動門檻/權重/維度定義。**"
  - round: 54
    line: 3304
    kind: bullet
    text: "- **不 push。**"
  - round: 55
    line: 3354
    kind: bullet
    text: |-
      - **不硬編「SBOM.json 必須存在」**、**不硬編 verify-system 的四步**：那是專案特定的
        宣告，寫進框架就破壞共通性。正解是讓 AC-N7.2 / AC-N12.x 自己走完
        SRS→TEST_SPEC→test 的鏈。R52 誠實聲明的 verify-system Goodhart 上限維持。
  - round: 55
    line: 3357
    kind: bullet
    text: |-
      - **`ac_parse_gap` 不升為 error。** 本站自己的紅測試主張要升，**那條紅測試是錯的**：
        既有的 `test_identifiers_outside_a_readable_shape_are_reported_as_unread` 早就把它
        釘在 `info`，理由是「框架讀不到某個形狀是框架的債」（R32 站4），這是對的。要擋的
        棄權是另一句話，落在 `check_ac_test_spec_coverage`。**改的是紅測試，不是既有規則。**
  - round: 55
    line: 3396
    kind: bullet
    text: |-
      - **不改 `check_coverage_report` 的靜默放行**（同檔、同形狀、不同一條）。
        再開條件：下一輪處理 P4 產物數字時一併裁。
  - round: 55
    line: 3398
    kind: bullet
    text: |-
      - **不改 `constitution/runner.py` 的 stub→100**。再開條件：constitution 重新進入
        自動管線的那一天。
  - round: 55
    line: 3400
    kind: bullet
    text: "- ~~**`check_cross_artifact` 只在 P3–P4 執行**~~ —— **站6 撤銷這一條並修掉它**，見下。"
  - round: 55
    line: 3401
    kind: bullet
    text: "- **不做 `forbidden` 契約的內容對賬**。再開條件：SAB 帶結構化禁令欄位。"
  - round: 55
    line: 3402
    kind: bullet
    text: "- **不修改任何 taskq-* 專案，不重判既有 gate 結果，不動門檻/權重/維度定義。**"
  - round: 55
    line: 3403
    kind: bullet
    text: "- **不 push。**"
  - round: 56
    line: 3566
    kind: bullet
    text: "- **不改 `_coverage_for_paths` 的算式**（R56-6 被推翻）。Re-open：任何專案開 branch coverage。"
  - round: 56
    line: 3567
    kind: bullet
    text: "- **不改 `ast-*` 的 `check_cmd`**。Re-open：AST 掃描器取得 harness 以外的依賴。"
  - round: 56
    line: 3568
    kind: bullet
    text: |-
      - **不改 `harness_bridge._TOOL_OUTPUT_PATTERNS` 的 `documented`**——它對
        `ast-docstrings` 與 `js-doc-coverage` 都寫 `"documented"`，而兩個掃描器都發 `with_doc`。
        是 OR 清單，`"total"` / `"missing"` 仍命中，無活傷口。Re-open：該清單改成 AND，
        或有掃描器只發它自己那一個鍵。
  - round: 56
    line: 3572
    kind: bullet
    text: "- **不合併 `lang_scanners` 與 `toolchains` 兩個掃描器家族**（範圍遠超本次 review）。"
  - round: 56
    line: 3573
    kind: bullet
    text: |-
      - **不放寬 `_AC_ID` 去吃 `AC-NFR01.1` / `AC-FR-01.a`**（貼合語料）。
        Re-open：那兩個專案把編號改成 canonical 之後仍讀不到。
  - round: 57
    line: 3659
    kind: bullet
    text: |-
      * **不改 `integration_coverage` 的量測方式**——本輪只把它移出 per-FR 分支。
        再開：有專案把 `integration_coverage` 放進 Gate 1 的維度表。
  - round: 57
    line: 3661
    kind: bullet
    text: |-
      * **不把兩條 `03-development/` glob 改對而是刪掉**——`compute_community_cohesion_score`
        已有 name-based 與 path-based 兩層排除涵蓋 `tests/`，補一條同義的是第二份陳述。
        再開：本 repo 出現需要排除而兩層規則都涵蓋不到的 community。
  - round: 57
    line: 3664
    kind: bullet
    text: "* **不動 `_effective_threshold` 的三來源取捨**（R27-DEFER-1，仍未裁決）。"
  - round: 57
    line: 3665
    kind: bullet
    text: "* **不合併 `lang_scanners` 與 `toolchains` 兩個掃描器家族**（範圍遠超本次 review）。"
  - round: 57
    line: 3666
    kind: bullet
    text: |-
      * **`_dim_passes` 不承擔「框架自己量不出來」的路由**——那是
        `SCORE_SOURCE_AGENT_UNVERIFIED` 與 S4 `unverifiable` 清單的職責，兩者都已用正確的
        owner 阻擋。再開：出現一個繞過那兩條、只在 `_dim_passes` 現身的案例。
  - round: 60
    line: 3804
    kind: bullet
    text: "- 不加 SHA lint（見 D4）。"
  - round: 60
    line: 3805
    kind: bullet
    text: "- 不修 doctor 的 dimension-scope drift（隨機制消失，撤回）。"
  - round: 60
    line: 3806
    kind: bullet
    text: "- 不改 taskq-\\* 任何檔案、不重判既有 gate 結果。"
  - round: 60
    line: 3807
    kind: bullet
    text: "- 不新增自動修復引擎，只接既有 R47/R48 路由。"
  - round: 60
    line: 3808
    kind: bullet
    text: "- 不動 `security_design` / `cross_artifact_live_cov`（不對應維度）。"
  - round: 60
    line: 3809
    kind: bullet
    text: "- 不改 mutmut 預算機制。**再開條件**：受影響專案移除鍵後實測跑不完。"
  - round: 86
    line: 4092
    kind: table_row
    item: "5 處 `startsWith('FILE_MISSING')` 是死檢查"
    reason: "`loadFileViaPython` 唯一的失敗回傳是 `'ERROR: LOADER_FAILED_AFTER_…'`,`file_loader` 的 status 是 `MISSING`,兩者都不以 `FILE_MISSING` 開頭。無關本輪病灶"
    reopen: "有人要重整 loader 的失敗回傳詞彙時一併收"
  - round: 86
    line: 4093
    kind: table_row
    item: "造假 agent 的信封偽造"
    reason: "信封擋的是**截斷**。一個從沒讀過檔案的 agent 可以自造一致的假 sha;JS 無 crypto、無頻外通道,單次 dispatch 內無解"
    reopen: "出現一次可證的偽造事故 → 改雙 dispatch(第二次只 relay ~400 bytes 的 JSON)"
  - round: 86
    line: 4094
    kind: table_row
    item: "`RELAY_MAX_BYTES` 量的是本機 Bash 工具,不是 sub-agent 的"
    reason: "兩者是否同一限制未直接觀測。方向安全:信封讓猜太高只會 fail-closed,猜太低只是少用全文模式"
    reopen: "取得 sub-agent 的實測上限時重新定值"
  - round: 86
    line: 4095
    kind: table_row
    item: "omnibot-new 的 341KB / 28 FR 是否該拆成多個 milestone"
    reason: "專案端範圍決定,不是框架決定。**修好 relay 是它 P1 的必要非充分條件** —— A 仍要在自己的 context 裡消化 341KB(≈9.5 萬 token)並寫出 SRS"
    reopen: "第一次真跑 P1 之後"
  - round: 86
    line: 4096
    kind: table_row
    item: "`cli/project_cmds.py:706` 的 `^###\\s+FR-(\\d+)\\s*:` 與 `structural_fr_ids` 是兩份實作"
    reason: "收編會改變 P1 fallback 的行為(多認 table/json 形式)—— Round 84 已列為明列不做,本輪不動"
    reopen: "同 Round 84"
  - round: 85
    line: 4187
    kind: table_row
    item: "env-check halt 把 `rc` 當 `--exit-code` 送進 `record-block`"
    reason: "`envReport.rc` 可能是 0（chain 成功但 `ready:false`），而 `OWNER_BY_EXIT[0] = Owner.NONE`「不是失敗」—— 比今天的 UNKNOWN 更糟"
    reopen: "有 `rc` 語意的實測資料，能證明 0 不出現在 halt 路徑上"
  - round: 85
    line: 4188
    kind: table_row
    item: "輪詢子代理的 turn budget 撐不撐得住 cap 次呼叫"
    reason: "生成 JS 用 runtime 提供的 `agent()`，本 repo 讀不到它的 turn 上限，無法量測。本輪把次數從 96 降到 40，方向上更安全"
    reopen: "有一次真實 run 的 dispatch 記錄可對照"
  - round: 85
    line: 4189
    kind: table_row
    item: "`render_env_check` 的 `Cap 22 polls` 仍是字面量"
    reason: "它輪詢的是 `run-env-check`，budget 來自 `STALL_TIMEOUTS` 而非 `fr_step × max_fix_rounds`，是另一條 SSOT"
    reopen: "該 cap 被量到不足時"
  - round: 85
    line: 4190
    kind: table_row
    item: "`recordBlock` 在 fail-closed 分支硬寫 `'phase-incomplete'`，不讀 `outcome.halt_step`"
    reason: "既有不對稱（halt 讀、ledger 不讀），與本輪病灶無關"
    reopen: "需要用 halt_step 做賬本聚合時"
  - round: 83
    line: 4634
    kind: table_row
    item: "把 `_check_git_sync` 的裸 `subprocess.run(timeout=)` 轉成 `run_isolated`"
    reason: "早於 R66 規則,在 ratchet 往下數的 92 個裡;順手改是鄰近程式碼改動,要有自己的理由"
    reopen: "該檔進入下一輪,或 ratchet 降到需要它"
  - round: 83
    line: 4635
    kind: table_row
    item: "SRS `C-xx` → SAB `architecture_constraints` 的 join"
    reason: "那張表只有 3/10 個專案使用,且與 SAB 的執行器名字不是同一套詞彙 —— 沒有可對的 id 映射。詳見站3c"
    reopen: "框架自己定義 `C-xx` 的結構與語意,或 SAB 改成收 SRS 的約束 id"
  - round: 83
    line: 4636
    kind: table_row
    item: "拆分 `core/quality_gate/artifact_consistency.py`(977 行,越過 900 門檻)"
    reason: "本輪主題是驗收鏈,不是檔案拆分;拆之前要先織位元組網(R49)"
    reopen: "該檔進入下一輪拆分清單"
  - round: 83
    line: 4637
    kind: table_row
    item: "給 `crash-triage` 一個「這不是 bug」的處置"
    reason: "唯一會寫 `.triaged` 的路徑是 `--open-cr`,會為 fixture 殘骸開 CR-BUG。這 69 份是一次性殘骸,為它加旗標是為單一情況造通用方案"
    reopen: "再出現一批非 bug 的 bundle,或框架自己的測試又把 bundle 寫進真 repo"
  - round: 83
    line: 4638
    kind: table_row
    item: "讓 `performance` 讀專案宣告的 p95 預算"
    reason: "`_score_pytest_benchmark` 用固定 1000/3000ms 罰則、不讀任何 p95(R46 站3 已記載)。要讀專案預算需要一條 p95 的機器可讀通道,是另一輪的設計"
    reopen: "出現 p95 的機器可讀來源,或再有一份報告把 `performance: 100.0` 讀成「達成 NFR-01」"
  - round: 83
    line: 4639
    kind: table_row
    item: "補上 `.github/workflows/harness_quality_gate.yml`(doctor 的第二個 WARN)"
    reason: "那是框架 repo 對自己部署消費專案 CI 模板的問題,與本輪三問無關;順手做是廚房水槽"
    reopen: "該 WARN 造成一次實際漏檢"
  - round: 83
    line: 4640
    kind: table_row
    item: "關掉 `git push --no-verify`"
    reason: "hook 關不掉(git 根本不叫它),branch protection 老闆禁止我動"
    reopen: "老闆解除 branch protection 的限制"
  - round: 82
    line: 4955
    kind: table_row
    item: "`GateContext`(170 行)搬進 `gate_result`"
    reason: "零個 stage 讀它。把沒人需要的東西搬進中立模組,正是中立模組長成第二個 god file 的方式"
    reopen: "有第二個模組需要它"
  - round: 82
    line: 4956
    kind: table_row
    item: "把 `_FinalizeStages` 變成真正的型別(Protocol / ABC)"
    reason: "它是命名空間。今天唯一的子類是 `HarnessBridge`,而 `if TYPE_CHECKING` 的宿主宣告已經把契約寫下來"
    reopen: "出現第二個宿主"
  - round: 82
    line: 4957
    kind: table_row
    item: "`cli/gate_cmds.py`(4000+ 行)與其他巨型檔"
    reason: "本輪的主題是「把 R81 抽出的 helper 搬出去」,不是全庫拆分。擴大範圍是廚房水槽"
    reopen: "該檔進入下一輪清單"
  - round: 82
    line: 4958
    kind: table_row
    item: "`monkeypatch` 雙命名空間的 helper 推廣到其他測試檔"
    reason: "站4 只在它實際咬人的那一個檔建了 helper。全庫掃描「哪些 patch 目標已經打不到呼叫端」是另一次審查 —— 而且是一次值得做的審查"
    reopen: "有人做那次審查"
  - round: 82
    line: 4959
    kind: table_row
    item: "賬本每一條的 `guard:` 逐條補齊"
    reason: "承 R81"
    reopen: "承 R81"
  - round: 82
    line: 4960
    kind: table_row
    item: "`test_test_spec_parser_parity` 的 `git ls-files` 時序"
    reason: "承 R81"
    reopen: "承 R81"
  - round: 82
    line: 4961
    kind: table_row
    item: "branch protection / `overlay_used` / mutmut 3.x / `RUNALL_MAX_BYTES`"
    reason: "承 R80、R81"
    reopen: "承 R80、R81"
  - round: 81
    line: 5297
    kind: table_row
    item: "Tier C/D 抽取(放寬到允許 1-3 個 live-out,實測可達 52-97%)"
    reason: "那些名字傳遞得對不對只能靠人看,而人看正是 R80 拒絕分解的理由。本輪只做「機械上可驗證」的那 39%"
    reopen: "出現一個能機械驗證單一 live-out 傳遞的方法"
  - round: 81
    line: 5298
    kind: table_row
    item: "`_precheck_p3_security_and_quality`(279)/ `_advance_step_commit_and_push`(257) 再拆"
    reason: "兩段在規則下**沒有安全切點**:每一個前綴都 bind 了後面要讀的東西。整段出來或都不出來"
    reopen: "這兩段內部的資料流改變,或有人為它們建行為 golden"
  - round: 81
    line: 5299
    kind: table_row
    item: "`cmd_run_fr_step` 剩下的 588 行"
    reason: "它把狀態穿過 dispatch 迴圈。這是那段程式碼的性質,不是方法的極限"
    reopen: "同上"
  - round: 81
    line: 5300
    kind: table_row
    item: "把抽出的 36 個 helper 搬去新模組"
    reason: "抽取已是本輪最大風險面;搬移是獨立且已被證明的動作(站2/站3)"
    reopen: "抽取全部在 main 上綠過一輪"
  - round: 81
    line: 5301
    kind: table_row
    item: "賬本每一條的 `guard:` 逐條補齊"
    reason: "只在有人真的看過、能指名時才填。逐條走完是另一次全庫審查"
    reopen: "有人做那次審查"
  - round: 81
    line: 5302
    kind: table_row
    item: "`test_test_spec_parser_parity` 的掃描讀 `git ls-files`,看不到未 commit 的新檔"
    reason: "本輪踩到一次(站5 的 commit 前綠、commit 後紅)。**不是本輪造的,且屬另一個守衛的家族**"
    reopen: "該守衛家族被系統性檢視,或有人因此漏掉一次真缺陷"
  - round: 81
    line: 5303
    kind: table_row
    item: "branch protection"
    reason: "老闆全域禁止。站4 修的是**另一個**洞"
    reopen: "—"
  - round: 81
    line: 5304
    kind: table_row
    item: "`overlay_used` 絕對路徑 / 支援 mutmut 3.x"
    reason: "承 R80"
    reopen: "承 R80"
  - round: 80
    line: 5497
    kind: table_row
    item: "`finalize_gate`(1150)/`cmd_run_fr_step`(940)/`cmd_advance_phase`(845)/`_advance_prechecks`(818) 的**分解**"
    reason: "區塊抽取會改動函式自身文字,byte-equal 規則不適用;實測 `harness_bridge.py` 全套件行為覆蓋 **81%**(1490 stmts / 282 未達),不足以證明抽取等價。站6 的 ratchet 已把它們凍結在現值只降不升 —— 與 `test_patch_discipline.py` 對 400 個 private patch 的處置同一理由"
    reopen: "有一支能釘住 `finalize_gate` 在 gate 矩陣上的 verdict / exit code / BLOCK 文字的行為 golden,且其覆蓋被實測"
  - round: 80
    line: 5498
    kind: table_row
    item: "`_crg_enrich_gate_findings` 搬出 harness_bridge"
    reason: "它的閉包會拉進 `_atomic_write_gate_result`(8 個呼叫點的共用寫入器)。搬那個是重構不是搬移,會造成 `gate_crg` ↔ `harness_bridge` 循環"
    reopen: "共用寫入器先被移到中立模組"
  - round: 80
    line: 5499
    kind: table_row
    item: "「明列不做」抽成機讀索引"
    reason: "23+ 條散在 5500 行散文裡,提取有把話塞進前幾輪嘴裡的實際風險。**代價已經發生**:本輪的 F7/F8/F10 就是因此被重新推導"
    reopen: "有人願意逐條與原文對照地做一次提取,或賬本改為結構化寫入"
  - round: 80
    line: 5500
    kind: table_row
    item: "`overlay_used` 仍是別台機器的絕對路徑"
    reason: "它現在不被任何東西比對、也不被任何東西讀。改寫是一次沒有可量測收益的位元變更"
    reopen: "出現一個讀者"
  - round: 80
    line: 5501
    kind: table_row
    item: "`RUNALL_MAX_BYTES` 的 153 bytes headroom(Round 58 留下)"
    reason: "不是本輪造的,且屬另一個 ratchet 家族"
    reopen: "該 ratchet 被系統性檢視時"
  - round: 80
    line: 5502
    kind: table_row
    item: "支援 mutmut 3.x"
    reason: "新功能不是修復;本輪只要求「版本不對就說不知道」"
    reopen: "老闆要求升級工具鏈"
  - round: 80
    line: 5503
    kind: table_row
    item: "branch protection"
    reason: "老闆全域禁止。**站3 的限制因此誠實記錄:它縮小視窗但不封閉它**"
    reopen: "—"
  - round: 80
    line: 5504
    kind: table_row
    item: "為框架建立真實 `.methodology/state.json`"
    reason: "老闆已裁示走「誁明不適用」"
    reopen: "老闆改變裁示"
  - round: 79
    line: 5645
    kind: table_row
    item: "重新診斷 runtime cache 跨 fresh launch 到底存不存在"
    reason: "我觀測不到。playbook §6.3 說範圍是同 session + `resumeFromRunId` + script 位元組不變;`4c24cf37` 說跨啟動存活;taskq-cc-new 的 spawn log **無法區分**(wrapper 不論 cache hit 與否都會寫記錄)。**本輪在兩種讀法下都正確**:站1 的移除兩種讀法下都對,站2 的 tag 不用就等同今天"
    reopen: "有人能在 runtime 端實測 cache 範圍"
  - round: 79
    line: 5646
    kind: table_row
    item: "`4c24cf37` 為什麼沒被 hook 擋"
    reason: "三個假設已排除,原因不明。站5 修的是我**能**證明的四條靜默路徑,不是一個未確認的原因"
    reopen: "老闆確認當時是否用了 `--no-verify`"
  - round: 79
    line: 5647
    kind: table_row
    item: "`git push --no-verify`"
    reason: "git 根本不叫 hook,機制關不掉。branch protection 是唯一能關的,老闆禁止我動"
    reopen: "—"
  - round: 79
    line: 5648
    kind: table_row
    item: "`test_amend_sab_appears_once_per_generated_workflow` 數字串不數 dispatch"
    reason: "別人的守衛,而且它已經為同樣理由帶了一個豁免(infra_abort return payload)。為自己的方便去放寬它正是 R64 的形狀 —— 站2 改的是自己的註解措辭"
    reopen: "有第三個正當 mention 出現時"
  - round: 79
    line: 5649
    kind: table_row
    item: "`'B Review'` / `'Persist Approval'` 兩個未宣告的 phase label"
    reason: "0978364c 就在了,不是本輪造的。替它們選一個 box 是改 progress view,不是 lint 該做的決定。站4 的守衛把它們列為**只減不增**的豁免"
    reopen: "有人決定這兩個 dispatch 該歸在哪個 box"
  - round: 79
    line: 5650
    kind: table_row
    item: "`core/ci_verdict.py` 的 `repo_slug(project) if runner is None else \"o/r\"`"
    reason: "`36d8c1d6` 沿用的是 `fetch_ci_verdict:128` 就有的既有形狀,不是新缺陷。代價是注入 runner 的測試永遠不會走到 `repo_slug`"
    reopen: "有人要處理 runner 注入 seam 的整體形狀"
  - round: 79
    line: 5651
    kind: table_row
    item: "`spec_shared.py:156` `_render_meta` 的 pyright「未使用」警告"
    reason: "HEAD 就有,與本輪無關"
    reopen: "—"
  - round: 76
    line: 5852
    kind: bullet
    text: |-
      - **JS/TS 沒有 per-FR scope。** `PER_TEST_OUTCOME_TOOLS` 逐名列出可讀的 runner,
        vitest/jest 刻意不在其中:框架自己的 JS run 是整套的,拿它去擋等於把本輪
        正在移除的缺陷原封不動搬給 JS 專案。條件寫在 MEASUREMENT_SINKS 的
        `reopen_when`。
  - round: 76
    line: 5856
    kind: bullet
    text: |-
      - **`cov_utils` / `gate_cmds` / `red_assertion_check` / `property_check` 的
        四種數字推導不合併。** 實測:對框架自己產得出來的每一個 FR id 全部一致,
        唯一分歧是 `FR-008`,而 `canonical_form("FR-008")` = `FR-08`,十個語料專案
        零個這種檔名。改了會動到不存在的檔案,是沒有量測支撐的行為變更 ——
        登記為潛伏兄弟(R74 慣例),不改。
  - round: 76
    line: 5861
    kind: bullet
    text: |-
      - **站5 情形 (a) 不擋。** 「少報 + 真的有紅」全部已被站1 的判定擋掉,(a) 若也擋
        只會擋到「多報」—— 而多報正是站3 之前的 prompt 教它寫的。同一輪剛拿掉歧義
        就用它扣分是 R42。re-open:站3 的 prompt 用過幾輪之後這種 row 還在出現。
  - round: 74
    line: 5989
    kind: table_row
    item: "站4 擋下 `[not checked]`"
    reason: "框架的模板從沒定義過那個 schema(`cross_cutting` / `fr_tests` 都沒有 layer),拿專案沒被要求寫的形狀去擋是 R42「合規的成本由被判定方承擔」"
    reopen: "站5 的模板出貨後,一個**新**專案(拿到新模板)仍把 NFR 測試寫進無 layer 的區段"
  - round: 74
    line: 5990
    kind: table_row
    item: "對既有九專案強制新 schema"
    reason: "`_init_copy_templates` 對已編輯檔案是 PROTECTED;`_entry_test_fn` 仍用內容定位認得四種舊拼法"
    reopen: "某專案的舊拼法造成判定錯誤而非僅僅少讀"
  - round: 74
    line: 5991
    kind: table_row
    item: "合併兩個 parser 的 section 語意"
    reason: "bridge 認 `### NFR-xx` 為可查詢的 section id,`_parse_test_spec` 把非 FR 標題 slug 化;合併會改變 Gate 1 per-FR cap 計入哪些列"
    reopen: "有專案的 per-FR cap 因兩者 section 語意不同而算錯"
  - round: 74
    line: 5992
    kind: table_row
    item: "`_flatten_test_names` 與 `tests:` 的雙向對賬"
    reason: "模板現在把 `fr_tests`/`cross_cutting` 定為 `tests:` 的**視圖**並用測試鎖住;生產端要求既有專案兩邊一致會擋住全部九個(taskq-new inventory 50 vs TEST_SPEC 115,而模板明說 inventory 可以是子集)"
    reopen: "一個專案的 `fr_tests` 名字不在 `tests:` 裡,且該名字沒有交付"
  - round: 73
    line: 6086
    kind: table_row
    item: "反造假旗標偵測(`--ignore` / `-k` / `--deselect` / `collect_ignore` / `testpaths` 移除)"
    reason: "那是「排除」不是「skip」;taskq-new 的 `--ignore=harness` 是合法用法(排除 vendored submodule),偵測它需要例外清單,而例外清單沒有對賬機制 —— 會複製本輪站3 的缺陷形狀"
    reopen: "一個專案用這五種形態之一把測試排除掉並拿到 gate PASS,且該形態無法用「合法排除 vendored 目錄」解釋"
  - round: 73
    line: 6087
    kind: table_row
    item: "站3 加入單數 `layer` 作為 keyword"
    reason: "taskq-api 的 `single_auth_dependency_at_api_layer` 會誤命中 layers candidate,而該專案有 layers contract → 會被判 `enforced`。**把棄權換成假背書比棄權更壞**(R72 站4 同一句,方向相反)"
    reopen: "出現一條真的由 layers contract 決定、而措辭只含單數 `layer` 的約束,且能與 `single_auth_…` 那類區分"
  - round: 73
    line: 6088
    kind: table_row
    item: "站5 擋下 `dimensions_declared_absent`"
    reason: "gate 的維度清單是框架的設計決定(`architecture_constraints` 是 per-FR 維度),擋下去會擋住每一個專案;NFR-06 的實質判定由站3 在 Gate 4 的 `unconfigured_blocking_reason` 承擔"
    reopen: "gate config 與 manifest 對某個維度分歧,而**沒有任何其他機制**判它 —— 那才是「需求零執行者」"
  - round: 73
    line: 6089
    kind: table_row
    item: "NFR-12 四步是否真的被串接"
    reason: "需要判斷自然語言列出的步驟是否出現在 recipe 裡,那是發明判準而非套用。站4 改為讓維度**不宣稱**它,站1 讓 `test_nfr12_*`(若被宣告)必須存在"
    reopen: "有專案宣告了 NFR-12 的 AC 測試、測試存在、卻仍與 recipe 不符"
  - round: 73
    line: 6090
    kind: table_row
    item: "~~`TEST_INVENTORY.yaml` 的欄位名與 NFR Layering Hard Rule 不符~~"
    reason: "**已於本輪站6 修復**(老闆追加指令),見下"
    reopen: "—"
  - round: 72
    line: 6293
    kind: table_row
    item: "A"
    reason: "`file:does-not-exist.db` 這類「檔名像 URI」的垃圾"
    reopen: "要發明一條關於檔名長相的規則,是發明判準不是套用正解。站7 已用一條測試把這個省略釘成「決定」而非疏漏"
  - round: 72
    line: 6294
    kind: table_row
    item: "B"
    reason: "`core.hooksPath=scripts/hooks` 沒有任何東西驗證它有設"
    reopen: "本輪兩次 CI 紅的直接原因是「push 前沒跑 self_check」,但那是流程紀律;hook 安裝與否是 clone 端的本地 git config,框架無法從 repo 內強制"
  - round: 72
    line: 6295
    kind: table_row
    item: "C"
    reason: "`gate:arch-constraints` 每次 gate 都寫一筆(taskq-new 191 筆)"
    reopen: "R54 已裁決 \"Recorded, never blocked\",理由成立;191 筆是 ledger 噪音而非判定缺陷"
  - round: 72
    line: 6296
    kind: table_row
    item: "D"
    reason: "`_scope_violation_scripts` 本身仍只看 untracked + top-level"
    reopen: "Surgical:站7 加的是**另一類**判準(工具備份後綴,全樹),既有那條的 top-level 限制有它自己的理由(遞迴會誤殺 mid-phase 的新模組)"
  - round: 70
    line: 6412
    kind: bullet
    text: |-
      - **不拆 `RUNALL_MAX_BYTES` 的註解**：單行 13,374 字元、14 筆歷史條目、
        有一支測試在 parse 它——用註解當資料庫是真問題，但拆檔要一併改那支測試，
        與本輪四站不相稱。**再開條件**：下一次有人需要在那行裡找一筆舊條目而找不到。
  - round: 70
    line: 6415
    kind: bullet
    text: |-
      - **不統一 D4 門檻的兩份表**：`spec_shared.D4_THRESHOLDS`（以 phase 索引）與
        `plangen._SPEC_COVERAGE_THRESHOLDS`（以 gate 索引）帶著同樣三個數字，
        分屬兩個互不 import 的 package。站1 只補上 gate 1 缺的那筆（40.0，原本只存在於
        GATE1 prompt 的 `--threshold 40.0` 字串裡），並用守衛把兩處綁住。
  - round: 70
    line: 6419
    kind: bullet
    text: |-
      - **Sync 站保留文字判定，且理由寫進碼裡**：`render_sync_verified` 跑的是前景
        `git push`，crash 發生在 pre-push hook 的子程序，`git push` 只會回 1——RC
        區分不了「hook 說 blocked」與「hook crash」。兩種來源對應兩種機制，不是漂移。
  - round: 70
    line: 6422
    kind: bullet
    text: "- **不改 `taskq-*` 任何檔案**：全程唯讀，只讀不寫。"
  - round: 69
    line: 6555
    kind: bullet
    text: |-
      - **不驗證 `Deferred:` 行指名的工具是否真的跑過。** 本輪只擋「沒指名」。
        再開條件＝`gate:ac-deferred` 累積到能看出哪些工具名是真的被執行的。
        這是 R43 母體在本輪的殘留。
  - round: 69
    line: 6558
    kind: bullet
    text: |-
      - **不 revert 那 8 個 commit。** 它們已 push 且各自解了真問題（taskq-new 59
        筆假指控、taskq-api P2 的 50 分鐘空轉、SRS prose 誤計）；正解是修在來源。
  - round: 69
    line: 6560
    kind: bullet
    text: |-
      - **AC 三態今天爆炸半徑為零**，且照實記：沒有語料專案寫過 `Deferred:` 行
        （Step 1d 才 shipped 幾小時），所以八個專案的違規數與站 3 逐筆相同，
        taskq-super 的 `AC-N7.2` 仍是 `ac_no_test_case`——它是 uncited，不是
        deferred。機制先於 prompt 的建議落到任何一棵樹。
  - round: 69
    line: 6564
    kind: bullet
    text: |-
      - **#1/E1 的後果強度是 Medium 不是 High。** 語料顯示 agent 在 taskq-cc 的 P6
        上確實自己重跑了 verify-gate（同一 commit 4 筆 verdict），所以現場行為
        比較可能是「昂貴、非確定性」而不是硬死鎖。修法在兩種情況下都正確，但
        **這不是死鎖，是每次都靠 agent 自己補救**，不冒充成更嚴重的戰果。
  - round: 69
    line: 6568
    kind: bullet
    text: |-
      - **站 1 的 re-verify 成本沒量。** 每個 exit-gate 相位多一次 verify-gate，
        含 `pip install code-review-graph` + CRG 圖建置。若過貴，替代方案是把
        verdict 記錄從 verify-gate 拆成獨立的輕量 `record-verdict`——更大的一輪。
  - round: 66
    line: 6945
    kind: unstructured
    heading: "### D 的前情：R25 曾明確「不做」，本輪以不同理由重開"
    spans_lines: [6945, 6952]
  - round: 65
    line: 7132
    kind: bullet
    text: |-
      - **不加 Windows 的 job object / taskkill /T**：無法在此驗證，且 CI 只有 ubuntu。
        `run_isolated` 在沒有 process group 的平台退回 `subprocess.run` 的行為並寫明。
        re-open：出現 Windows CI 或 Windows 上的實測工單。
  - round: 65
    line: 7135
    kind: bullet
    text: |-
      - **不動 `phase5-verification.js` 的 `tests/integration/` 硬編路徑**：
        同一類但屬先前既有，且該行自帶 "skip gracefully if dir absent"。
        re-open：有專案的 integration 測試不在 `tests/integration/`。
  - round: 65
    line: 7138
    kind: bullet
    text: |-
      - **不動 `--cov` 之外的 prompt 路徑陳述**（bug-hunt 的 "write a repro test under
        03-development/tests/"）：那是寫入位置的建議，不是被 Gate 3 重量的判準。
  - round: 65
    line: 7140
    kind: bullet
    text: |-
      - **不改 `suite_timeout` 的 30 秒下限**：站0 的逾時測試改 patch 這個旋鈕，
        其餘全是真的 pytest 執行。
  - round: 64
    line: 7285
    kind: bullet
    text: |-
      - **不改 SAB prompt 的 14/18 硬寫數字**（發現 8）：今天實測正確，
        且該句真正的指令不依賴這兩個數。**再開條件**：詞彙表變動。
  - round: 64
    line: 7287
    kind: bullet
    text: "- **不改「最後一次回空判 session block」的語意**（見 D2）。"
  - round: 64
    line: 7288
    kind: bullet
    text: "- **不追認 Round 62/63 兩個沒有賬本節的號碼**。"
  - round: 64
    line: 7289
    kind: bullet
    text: "- **不改 taskq-\\* 任何檔案、不重判既有 gate 結果。**"
