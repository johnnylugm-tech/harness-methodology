# SRS - Harness Methodology v1.0 (Standalone)

## 1. 導論 (Introduction)

### 1.1 專案願景
在 AI 驅動的軟體開發時代，傳統的開發流程已不足以確保產出的品質、安全性與可追溯性。本專案旨在建構一個名為 `Harness Methodology` 的新一代軟體開發框架，其核心使命是為 AI 代理人（特別是大型語言模型）的開發活動提供一套**具備高度工程紀律、流程標準化、且結果可被量化驗證的自動化治理體系**。

### 1.2 問題領域
當開發任務由 AI 代理人執行時，會面臨以下挑戰：
*   **品質不穩定**：AI 產出的程式碼品質可能存在波動。
*   **過程黑箱**：決策過程缺乏記錄，難以追溯與審計。
*   **安全風險**：AI 可能會無意中引入安全漏洞或執行危險操作。
*   **需求漂移**：實現的功能可能與最初的需求不完全對齊。
*   **驗證偏見**：若使用同一 AI 模型進行開發與審查，可能無法發現其自身的盲點。

`Harness Methodology` 框架正是為了解決以上所有問題而設計。

### 1.3 目標使用者與角色
本系統為軟體開發團隊中的不同角色而設計，每個角色都有其明確的職責與互動介面。系統應支援以下角色（`Personas`）：
*   **操作員 (Operator)**: 作為人類使用者，透過 CLI 發起指令，驅動整個開發流程。
*   **產品經理 (Product Manager)**: 負責在 P1 階段定義需求。
*   **架構師 (Architect)**: 負責在 P2 階段進行架構設計。
*   **開發者 (Developer)**: 負責在 P3 階段實現功能。
*   **品質保證工程師 (QA Engineer)**: 負責在 P4 和 P6 階段進行測試與品質驗證。
*   **開發維運工程師 (DevOps)**: 負責在 P5 和 P8 階段處理交付與設定管理。
*   **審查者 (Reviewer)**: 一個獨立的審查角色，用於對各階段的產出進行客觀驗證。

---

## 2. 功能性需求 (Functional Requirements)

### FR-1: 指令行介面 (Command-Line Interface)
系統應提供一個名為 `cli.py` 的統一指令行介面作為唯一入口，並應支援以下 12 個核心命令：

*   **FR-1.1**: 系統應提供 `plan-phase` 命令，用於為指定階段生成詳細的執行計畫文件。
*   **FR-1.2**: 系統應提供 `run-phase` 命令，用於執行計畫。
*   **FR-1.3**: 系統應提供 `stage-pass` 命令，用於在階段出口條件滿足時，生成一個加密的通行權杖。
*   **FR-1.4**: 系統應提供 `end-phase` 命令，用於標記階段的結束。
*   **FR-1.5**: 系統應提供 `update-step` 命令，用於更新計畫步驟的狀態。
*   **FR-1.6**: 系統應提供 `phase-verify` 命令，用於驗證階段交付物的完整性。
*   **FR-1.7**: 系統應提供 `trace-check` 命令，用於檢查需求的可追蹤性。
*   **FR-1.8**: 系統應提供 `enforce` 命令，用於在 P3 階段強制執行團隊憲法。
*   **FR-1.9**: 系統應提供 `auto-research` 命令，用於在 P7/P8 階段執行品質評估。
*   **FR-1.10**: 系統應提供 `quality-gate` 命令，作為執行四層品質門的核心入口。
*   **FR-1.11**: 系統應提供 `verify-artifact` 命令，用於驗證單一交付物的合規性。
*   **FR-1.12**: 系統應提供 `steering` 命令，用於在 P7/P8 階段啟動指導迴圈。

### FR-2: 標準化八階段工作流 (8-Phase Workflow)
系統應實現一個基於 ASPICE 標準的、從 P1 到 P8 的軟體開發生命週期。每個階段都必須有明確定義的輸入、標準化輸出、以及程式化的出口條件。

| Phase | 階段名稱 | 核心產出 (Artifact) |
|---|---|---|
| P1 | Requirements | `SRS.md` |
| P2 | Architecture | `SAD.md`, `quality_manifest.json` |
| P3 | Implementation | 原始碼, 單元測試 |
| P4 | Testing | `TEST_PLAN.md`, `TEST_RESULTS.md` |
| P5 | Delivery | `DEPLOYMENT.md`, `SPEC_TRACKING.md` |
| P6 | Quality Assurance | `QUALITY_REPORT.md` |
| P7 | Risk & Monitoring | `RISK_REGISTER.md`, `MONITORING_PLAN.md` |
| P8 | Configuration Mgmt | `CONFIG_RECORDS.md`, `BASELINE.md` |

### FR-3: 四層品質門 (4-Gate Quality System)
系統應實現一個四層品質門系統，在開發流程的關鍵節點強制執行品質檢查。

*   **FR-3.1 (Gate 1)**: 系統應在 P3/P5/P7/P8 的每個功能需求 (FR) 完成後，執行一次基礎品質檢查（包括 Linting, 型別安全, 測試覆蓋率）。
*   **FR-3.2 (Gate 2)**: 系統應在 P3 結束時，執行一次更深入的檢查，引入安全性掃描和變異測試。
*   **FR-3.3 (Gate 3)**: 系統應在 P4 結束時，執行首次全維度（12-dim）品質評估，包括架構、可讀性和效能。
*   **FR-3.4 (Gate 4)**: 系統應在 P6 結束時，對最終產品執行最嚴格的全維度品質總檢。
*   **FR-3.5**: 任何 Gate 的評分若未達到其設定的 `score_gate` 閾值，系統應阻塞工作流程，並可選擇性地啟動一個最多 3 輪的自動修復迴圈。

---

## 3. 非功能性需求 (Non-Functional Requirements)

### NFR-1: 可靠性 & 可重現性 (Reliability & Reproducibility)
*   **NFR-1.1 (G3)**: 系統的變異測試結果必須是可信的。系統應透過執行至少 3 次測試並取中位數結果的機制，來消除單次執行的隨機性波動。
*   **NFR-1.2**: 系統的品質評分必須具備確定性下限。對於可被確定性工具（如 Linter, 靜態分析, CRG）和大型語言模型（LLM）同時評估的維度，最終分數應取兩者中的較低值，以防止分數膨脹。

### NFR-2: 安全性 (Security)
*   **NFR-2.1**: 系統應包含一個 `Kill Switch` 機制。此機制應能在偵測到高風險操作或 AI 輸出不確定性超過安全閾值時，立即中斷當前任務。
*   **NFR-2.2**: 系統在執行任何自動程式碼修復前，必須進行影響分析。若 Code Review Graph (CRG) 分析顯示該修復的風險評分高於 0.7，則應自動推遲此修復，並將其標記為需要人工審查。

### NFR-3: 可追蹤性 (Traceability)
*   **NFR-3.1 (G5)**: 系統必須確保從需求到程式碼再到測試的全鏈路可追蹤。應提供一個機制，能將一個功能需求（FR ID）與其對應的原始碼位置、測試案例、以及在品質門中發現的所有相關問題進行雙向關聯。
*   **NFR-3.2 (G1)**: 系統必須在架構設計階段 (P2) 結束時，生成一份名為 `quality_manifest.json` 的數位合約。此文件應作為後續所有開發與測試活動的統一品質基準與追蹤依據。

### NFR-4: 驗證獨立性 (Validation Independence)
*   **NFR-4.1 (G2)**: 為徹底解決「開發者」與「審查者」使用同一 AI 模型可能產生的確認偏誤，系統應將「審查者」角色的執行與主框架分離。所有審查任務都必須透過一個標準化的外部訊息通道 (Hermes MCP) 發送給一個獨立的審查服務。
*   **NFR-4.2 (HR-01)**: 系統應在流程中強制執行「禁止自我審批」規則。
*   **NFR-4.3 (HR-06)**: 系統應禁止「審查者」代理人直接修改「開發者」代理人的工作產出，以確保權責分離。

### NFR-5: 可維護性 (Maintainability)
*   **NFR-5.1**: 系統的程式碼庫必須是簡潔且高度內聚的。所有與核心功能無關的程式碼都應被排除。
*   **NFR-5.2**: 框架的核心整合層 (`harness/` 模組) 必須包含自身的單元測試套件，且測試覆蓋率不得低於 80%。

### NFR-6: 文件化與可審計性 (Documentation & Auditability)
*   **NFR-6.1 (萃取自 #13)**: 系統必須為每一次 AI 代理人的關鍵決策（如 Gate 的通過/阻斷、程式碼的修改等）生成結構化的「決策日誌 (`DecisionLog`)」。日誌應以人類可讀的 YAML 格式儲存，便於後續審計。
*   **NFR-6.2 (G6)**: 系統應能根據專案的當前狀態，自動生成一份標準化的 `CLAUDE.md` 文件。此文件應作為人類操作員與 AI 代理人之間進行工作交接的標準介面，清晰地展示專案進度、風險和待辦事項。

---

## 4. 外部介面 (External Interfaces)
*   **EI-1 (Claude Code `Task` Tool)**: 系統的 `AgentSpawner` 模組將依賴此介面來執行所有「開發型」代理人（如 Developer, Architect）的任務。
*   **EI-2 (Hermes MCP)**: 系統的 `ReviewerRouter` 模組將依賴此訊息協定介面，與外部的「審查者」服務進行非同步通訊（發送審查請求、等待並接收審查結果）。此介面的目標位址必須可透過 `HERMES_REVIEWER_TARGET` 環境變數進行配置。
